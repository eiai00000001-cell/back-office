# 個人事業(EIAI TEC)向け事務管理システム テストケース

参照: `テスト仕様書.md`(版数3.1-stage1)。本書は仕様のみを一覧化し、実施結果は`テスト結果報告書.md`に記載する。

本書はイテレーションをまたいだ累積記載である。「共通〜F-02」の各章(TC-C/TC-S/TC-A/TC-H/TC-Q/TC-CV/TC-I/TC-P/TC-E/TC-N、計67件)はイテレーション1分であり、内容・版数据え置き(変更なし)。「F-07 財務ダッシュボード」章(TC-D、イテレーション2で新規追加)を追加した。イテレーション3・段階1として、F-08 案件・プロジェクト管理の章群(TC-J・TC-MG・TC-LN・TC-UI・TC-RG、計73件)を末尾に追加した(版数3.0-stage1)。

---

## 共通: 取引先マスタ管理(SC-10)

| ID | 機能ID | 観点 | テスト内容 | 事前条件 | 手順 | 期待結果 |
|---|---|---|---|---|---|---|
| TC-C01 | 共通 | 機能テスト | 取引先新規登録(正常系) | DBクリーン | `POST /api/clients`に名称・郵便番号・住所・担当者名・連絡先を指定して送信 | 201応答、登録内容がレスポンスに反映される |
| TC-C02 | 共通 | 異常系 | 名称未入力エラー | - | `POST /api/clients`に`{"name":""}`を送信 | 422応答、`detail`が単一の日本語文字列「名称を入力してください」 |
| TC-C03 | 共通 | 異常系 | 郵便番号形式不正エラー | - | `POST /api/clients`に`postal_code:"ABC-DEFG"`を送信 | 422応答でエラーになる |
| TC-C04 | 共通 | 機能テスト | 取引先一覧取得 | 取引先1件以上登録済み | `GET /api/clients` | 200応答、登録済み取引先が配列で返る |
| TC-C05 | 共通 | データ整合性 | 取引先更新 | 取引先登録済み | `PUT /api/clients/{id}`で名称を変更 | 200応答、DB上の値が更新後の内容になる |
| TC-C06 | 共通 | 結合確認 | 取引先マスタ画面(SC-10)への遷移導線の存在確認 | - | (a) ホーム画面(SC-01)に取引先マスタへの遷移導線があるか確認 (b) 請求書詳細(SC-03)・見積書詳細(SC-05)の取引先選択欄に、mockup(SC-03/SC-05)で規定された「新規登録」インラインリンクがあるか確認 | mockup SC-03/SC-05どおり、取引先選択欄付近に「新規登録」リンクがあり、クリックでSC-10へ遷移し、保存後に呼び出し元へ戻ること |

---

## 共通: システム設定(発行者情報、SC-11)

| ID | 機能ID | 観点 | テスト内容 | 事前条件 | 手順 | 期待結果 |
|---|---|---|---|---|---|---|
| TC-S01 | 共通 | 機能テスト | 発行者情報取得 | - | `GET /api/company-profile` | 200応答 |
| TC-S02 | 共通 | 機能テスト | 発行者情報更新(登録番号あり) | - | `PUT /api/company-profile`に氏名・屋号・住所・連絡先・登録番号(`T`+13桁)を送信 | 200応答、内容が反映される |
| TC-S03 | 共通 | 異常系 | 登録番号形式不正エラー | - | `PUT /api/company-profile`に`invoice_registration_number:"XYZ123"`を送信 | 422応答でエラーになる |
| TC-S04 | 共通 | 結合確認 | ホーム画面からシステム設定への導線 | - | ホーム画面右上の「システム設定」リンクの実装を確認 | ホーム画面のみ(戻り先がない画面)に「システム設定」リンクが表示され、SC-11へ遷移すること |

---

## 共通: アプリケーション起動・終了(`起動.app`/`終了.app`)

| ID | 機能ID | 観点 | テスト内容 | 事前条件 | 手順 | 期待結果 |
|---|---|---|---|---|---|---|
| TC-A01 | 共通 | 機能テスト | 起動.appによる新規起動 | Uvicorn未起動、ポート8000未使用 | `dist/起動.app`をmacOSの`open`コマンドで起動(ダブルクリック相当)。最大15秒待機 | Uvicornがバックグラウンド起動し、`run/uvicorn.pid`が作成され、既定ブラウザで`http://127.0.0.1:8000/`が開き、SPAが読み込まれる(`GET /`・JSバンドル・`/api/home/summary`が200で応答) |
| TC-A02 | 共通 | 機能テスト | 起動.appの二重起動時の冪等性 | Uvicorn起動中 | 起動中の状態で再度`起動.app`を起動 | ヘルスチェックが200のため新規プロセスを起動せず、既存プロセスのままブラウザが開く(プロセスが重複しない) |
| TC-A03 | 共通 | 機能テスト | 終了.appによる正常終了 | Uvicorn起動中 | `dist/終了.app`を起動 | SIGTERMでプロセスが終了し、`run/uvicorn.pid`が削除され、`osascript`で「システムを終了しました」ダイアログが表示される |
| TC-A04 | 共通 | 異常系 | 終了.appを未起動時に実行 | Uvicorn未起動、PIDファイルなし | `dist/終了.app`を起動 | プロセス停止処理は行わず、`osascript`で「システムは起動していません」ダイアログが表示される |
| TC-A05 | 共通 | 異常系 | 起動失敗時(15秒タイムアウト)の通知 | - | (意図的な起動失敗の再現は本番環境を破壊しうるため未実施。`launch.sh`のタイムアウト分岐コードの静的確認に留める) | `osascript`で「起動に失敗しました。logs/uvicorn.logを確認してください」ダイアログが表示され、ブラウザは開かない |

---

## F-06 ホーム画面・共通UI(SC-01)

| ID | 機能ID | 観点 | テスト内容 | 事前条件 | 手順 | 期待結果 |
|---|---|---|---|---|---|---|
| TC-H01 | F-06 | データ整合性 | ホームサマリーの件数整合性 | 請求書データ複数(未入金・一部入金・完済・期限超過混在) | `GET /api/home/summary`と`GET /api/invoices`の集計を突き合わせる | `unpaid_count`=UNPAID/PARTIALLY_PAIDの件数と一致、`overdue_count`=is_overdue=trueの件数と一致 |
| TC-H02 | F-06 | 結合確認 | ホーム画面のナビゲーションカード | - | ホーム画面の導線カード実装をmockup(SC-01)と突き合わせ | 請求書・経費管理・見積書・売掛金の4カードが表示され、各々SC-02/SC-06/SC-04/SC-09へ遷移する |
| TC-H03 | F-06 | 異常系 | サマリー取得API失敗時の表示分離 | - | サマリー表示エリアのエラー時分岐コードを確認(`isError`時に専用のエラー表示のみ差し替え) | サマリーエリアのみエラー表示となり、ナビゲーションカードの表示・操作には影響しない |

---

## F-03 見積書作成(SC-04・SC-05)

| ID | 機能ID | 観点 | テスト内容 | 事前条件 | 手順 | 期待結果 |
|---|---|---|---|---|---|---|
| TC-Q01 | F-03 | データ整合性 | 見積書新規作成・税額計算(標準10%+非課税混在) | 取引先登録済み | `POST /api/quotes`に数量2.5×単価10,000(標準10%)、数量1×単価5,000(非課税)の2明細で送信 | 201応答。小計=30,000円、消費税額=floor(25,000×0.10)=2,500円、合計=32,500円 |
| TC-Q02 | F-03 | 機能テスト | 見積書番号の自動採番フォーマット | - | 新規作成後の`quote_number`を確認 | `YYYY-NNNN`形式(西暦4桁-連番4桁ゼロ埋め) |
| TC-Q03 | F-03 | 異常系 | 品目明細0件エラー | - | `items:[]`で保存 | 400応答、「品目明細を1件以上入力してください」相当のエラー |
| TC-Q04 | F-03 | 異常系 | 数量0エラー | - | `quantity:"0"`で保存 | 422応答、`detail`=「数量は0より大きい数値で入力してください」 |
| TC-Q05 | F-03 | 異常系 | 取引先未選択エラー | - | `client_id`を欠落させて保存 | 422応答でエラーになる |
| TC-Q06 | F-03 | 機能テスト | 見積書一覧・取引先フィルタ | 見積書登録済み | `GET /api/quotes?client_id={id}` | 200応答、指定取引先の見積書のみ返る |
| TC-Q07 | F-03 | 機能テスト | ステータス切替(DRAFT→CONFIRMED) | 見積書登録済み(DRAFT) | `PUT /api/quotes/{id}`で`status:"CONFIRMED"` | 200応答、`status`が`CONFIRMED`に更新される |
| TC-Q08 | F-03 | 機能テスト | 見積書PDF出力 | 見積書登録済み | `GET /api/quotes/{id}/pdf` | 200応答、`application/pdf`、有効なPDFバイナリ(`%PDF`ヘッダ)が返る |
| TC-Q09 | F-03 | 静的確認 | PDFレイアウトの内容(登録番号欄・税率別区分・適用税率) | - | `pdf_generation_service.py`・`pdf_base.html`を詳細設計書4.1ステップ10と突き合わせ | 登録番号欄(未入力時空欄)、税区分別(標準10%/非課税/不課税)の小計、「消費税額(標準10%)」の適用税率表記が実装されている |
| TC-Q10 | F-03 | 結合確認 | 見積書明細のリアルタイム金額表示 | - | `ItemsEditor`コンポーネントの実装を確認(`calculateTotals`/`calculateItemAmount`) | 数量・単価変更時にサーバー通信なしで小計・税額・合計がリアルタイム表示される(フロントとサーバーで同一計算ロジック) |

---

## F-04 見積→請求の自動変換

| ID | 機能ID | 観点 | テスト内容 | 事前条件 | 手順 | 期待結果 |
|---|---|---|---|---|---|---|
| TC-CV01 | F-04 | 機能テスト | 見積書→請求書変換の正常系 | 見積書登録済み(TC-Q01のもの) | `POST /api/quotes/{id}/convert-to-invoice` | 201応答、`issue_date`/`due_date`がNULL、`source_quote_id`が変換元見積書ID、金額(小計・税額・合計)が見積書から引き継がれる |
| TC-CV02 | F-04 | 異常系 | 変換済み見積書の再変換拒否 | TC-CV01実施済み | 同じ見積書IDで再度変換APIを実行 | 409応答、「この見積書は既に請求書(No.XXX)に変換済みのため、再変換できません」 |
| TC-CV03 | F-04 | 結合確認 | 変換後の独立性(請求書編集が見積書に影響しない) | TC-CV01実施済み | 変換後の請求書の品目・金額を編集し保存。元見積書を再取得 | 見積書側の金額・明細は変換前のまま変化しない |
| TC-CV04 | F-04 | 結合確認 | 見積書詳細への変換先請求書番号の反映 | TC-CV01実施済み | 見積書詳細(`GET /api/quotes/{id}`)を取得 | `converted_invoice_id`/`converted_invoice_number`が変換後の請求書と一致する |
| TC-CV05 | F-04 | 異常系 | 存在しない見積書の変換 | - | `POST /api/quotes/999999/convert-to-invoice` | 404応答 |
| TC-CV06 | F-04 | 結合確認 | 変換直後の請求書詳細画面での注意表示 | TC-CV01実施済み | 変換直後の請求書詳細画面(SC-03)の実装を確認(`issue_date`/`due_date`がnullの場合の分岐) | 「発行日・支払期限が未設定です」の警告表示が行われる |

---

## F-01 請求書発行(SC-02・SC-03)

| ID | 機能ID | 観点 | テスト内容 | 事前条件 | 手順 | 期待結果 |
|---|---|---|---|---|---|---|
| TC-I01 | F-01 | 機能テスト | 請求書新規作成(通常、発行日・支払期限あり) | 取引先登録済み | `POST /api/invoices`に発行日・支払期限・明細を指定して送信 | 201応答、`invoice_number`が`YYYY-NNNN`形式で採番される |
| TC-I02 | F-01 | 異常系 | 通常保存時の発行日未入力エラー | - | `issue_date`を欠落させてF-04変換経由でない新規作成を実行 | 400応答でエラーになる(F-04直後の例外は適用されない) |
| TC-I03 | F-01 | 機能テスト | 請求書PDF出力 | 請求書登録済み | `GET /api/invoices/{id}/pdf` | 200応答、有効なPDFバイナリが返る |
| TC-I04 | F-01 | 機能テスト | 請求書一覧・取引先フィルタ | 請求書複数登録済み | `GET /api/invoices?client_id={id}` | 200応答、指定取引先の請求書のみ返る |
| TC-I05 | F-01 | 異常系 | 数量・単価0以下エラー | - | `quantity:"0"`または`unit_price:0`で保存 | 422応答、該当の日本語エラーメッセージが返る |
| TC-I06 | F-01 | 異常系 | 品目明細0件エラー | - | `items:[]`で保存 | 400応答でエラーになる |
| TC-I07 | F-01 | データ整合性 | 請求書更新後の金額再計算 | 請求書登録済み | `PUT /api/invoices/{id}`で明細内容を変更 | 更新後の小計・税額・合計がDBに正しく反映される |

---

## F-05 入金・売掛金管理(SC-03入金記録・SC-09)

| ID | 機能ID | 観点 | テスト内容 | 事前条件 | 手順 | 期待結果 |
|---|---|---|---|---|---|---|
| TC-P01 | F-05 | データ整合性 | 入金前ステータス | 請求書登録済み(入金なし) | `GET /api/invoices/{id}` | `payment_status`=`UNPAID` |
| TC-P02 | F-05 | 機能テスト | 支払期限超過検知 | 支払期限が過去日付の未入金請求書 | `GET /api/invoices/{id}` | `is_overdue`=`true` |
| TC-P03 | F-05 | 機能テスト | 一部入金登録 | 請求書登録済み | `POST /api/invoices/{id}/payments`で請求額未満の金額を登録 | 201応答 |
| TC-P04 | F-05 | データ整合性 | 一部入金後のステータス | TC-P03実施済み | `GET /api/invoices/{id}` | `payment_status`=`PARTIALLY_PAID` |
| TC-P05 | F-05 | 異常系 | 入金額0円エラー | - | `amount:0`で入金登録 | 422応答、「入金額は0より大きい数値で入力してください」 |
| TC-P06 | F-05 | 異常系 | 入金超過時の確認要求(force未指定) | 残額より大きい入金額 | `force`を指定せず入金登録 | 409応答、「入金額の合計が請求金額を超過します。登録しますか?」 |
| TC-P07 | F-05 | 機能テスト | 入金超過をforce=trueで登録許可 | TC-P06と同条件 | `force:true`で再送信 | 201応答、登録される |
| TC-P08 | F-05 | データ整合性 | 完済後のステータス・超過表示解除 | TC-P07実施済み(合計が請求額以上) | `GET /api/invoices/{id}` | `payment_status`=`PAID`、`is_overdue`=`false` |
| TC-P09 | F-05 | 機能テスト | 入金記録一覧取得 | 入金記録複数 | `GET /api/invoices/{id}/payments` | 登録した入金記録が全件返る |
| TC-P10 | F-05 | 機能テスト | 入金記録削除 | 入金記録登録済み | `DELETE /api/payments/{id}` | 204応答 |
| TC-P11 | F-05 | データ整合性 | 入金削除後のステータス再算出 | TC-P10実施済み | `GET /api/invoices/{id}` | `payment_status`が`payments`テーブルの残存データから都度再計算される(DBに保存されない派生値であることの確認) |
| TC-P12 | F-05 | 結合確認 | 売掛金一覧のステータスフィルタ | 完済請求書・未入金請求書が混在 | `GET /api/invoices?payment_status=UNPAID` | 完済(`PAID`)の請求書は結果に含まれない |
| TC-P13 | F-05 | 結合確認 | 支払期限超過行の強調表示 | 期限超過請求書あり | 売掛金一覧画面(SC-09)・請求書一覧画面(SC-02)の実装(`is_overdue`に応じた背景色・強調表示)を確認 | 期限超過の行が視覚的に強調表示される |

---

## F-02 経費管理(SC-06・SC-07・SC-08)

| ID | 機能ID | 観点 | テスト内容 | 事前条件 | 手順 | 期待結果 |
|---|---|---|---|---|---|---|
| TC-E01 | F-02 | 機能テスト | 経費新規登録(正常) | - | `POST /api/expenses`に発生日・勘定科目・金額等を送信 | 201応答 |
| TC-E02 | F-02 | 異常系 | 金額0円エラー | - | `amount:0`で登録 | 422応答でエラーになる |
| TC-E03 | F-02 | 異常系 | 発生日未入力エラー | - | `expense_date:""`で登録 | 422応答でエラーになる |
| TC-E04 | F-02 | 異常系 | 期間検索From>Toエラー | - | `GET /api/expenses?date_from=2026-09-20&date_to=2026-09-01` | 400応答でエラーになる |
| TC-E05 | F-02 | 機能テスト | 勘定科目フィルタ検索 | 経費登録済み | `GET /api/expenses?account_category=消耗品費`(日本語はURLエンコード必須) | 200応答、指定科目の経費のみ返る |
| TC-E06 | F-02 | 機能テスト | 領収書添付アップロード(正常、jpg) | 経費登録済み | `POST /api/expenses/{id}/attachment`にjpgファイルを送信 | 200応答、`attachment_path`が設定される |
| TC-E07 | F-02 | 異常系 | 領収書添付・対応外拡張子エラー | 経費登録済み | `.exe`ファイルをアップロード | 400応答、「対応していないファイル形式、またはサイズが上限(10MB)を超えています」相当のエラー |
| TC-E08 | F-02 | 機能テスト | 領収書添付ファイル取得 | 添付済み | `GET /api/expenses/{id}/attachment` | 200応答、ファイル本体が返る |
| TC-E09 | F-02 | データ整合性 | 経費集計(勘定科目別・月別) | 経費登録済み | `GET /api/expenses/summary?period_from=YYYY-MM&period_to=YYYY-MM` | 集計金額が登録データの合計と一致する |
| TC-E10 | F-02 | データ整合性 | 経費削除時の添付ファイル実体の連動削除 | 添付ファイルあり | `DELETE /api/expenses/{id}`実行後、ファイルシステム上の該当ディレクトリを確認 | DBレコードとともに、`data/attachments/expenses/{id}/`配下のファイル実体も削除される(孤立ファイルが残らない) |
| TC-E11 | F-02 | 異常系 | 領収書添付・サイズ上限(10MB)超過エラー | - | `AttachmentService.validate()`の実装(サイズ判定条件)を静的確認 | 10MB超過時にエラーとなるロジックが実装されている(pytestで検証済み、README参照) |

---

## 異常系共通・404確認

| ID | 機能ID | 観点 | テスト内容 | 事前条件 | 手順 | 期待結果 |
|---|---|---|---|---|---|---|
| TC-N01 | 共通 | 異常系 | 存在しない請求書の取得 | - | `GET /api/invoices/999999` | 404応答 |
| TC-N02 | 共通 | 異常系 | 存在しないリソースへのその他アクセス | - | 存在しない見積書ID・経費IDへのGET/PUT/DELETE | いずれも404応答 |

---

## F-07 財務ダッシュボード(SC-12、イテレーション2)

| ID | 機能ID | 観点 | テスト内容 | 事前条件 | 手順 | 期待結果 |
|---|---|---|---|---|---|---|
| TC-D01 | F-07 | 機能テスト/境界値 | データ0件時の4区画API出力(欠損期間の0円・0件補完) | 請求書・見積書・経費・入金がいずれも0件 | `GET /api/dashboard/sales-and-payments`・`/expenses`・`/profit-loss`・`/quotes`を実行 | いずれも200応答。`sales`・`payments`・経費`monthly`・損益`monthly`・見積`monthly`が12件(直近12ヶ月分)の配列で、全月`amount`=0(見積は`count`=0・`total_amount`=0)。経費`by_category`は空配列。見積`conversion_rate`は`null` |
| TC-D02 | F-07 | 異常系 | 読み取り専用であることの確認(書き込みメソッド拒否) | - | `POST`/`PUT`/`DELETE`で4エンドポイントそれぞれにリクエスト | いずれも405応答(Method Not Allowed)。書き込み用エンドポイントが存在しないこと |
| TC-D03 | F-07 | データ整合性/結合確認 | 一部月のみ実績がある場合の月次売上・経費・損益集計(発生主義) | 直近12ヶ月のうち一部の月にのみ、請求書(発行日あり)・経費(発生日あり)を登録済み | `GET /api/dashboard/sales-and-payments`・`/expenses`・`/profit-loss`を実行し、DB上の`invoices.issue_date`・`expenses.expense_date`の実データ集計と突き合わせる | 実績のある月のみ`amount`が実データ合計と一致し、実績のない月は0円で補完される。損益は当該月の売上−経費と一致する |
| TC-D04 | F-07 | データ整合性/境界値 | 対象期間開始日(`date_from`)前日のデータが集計対象から除外される | 直近12ヶ月の開始月の前月末日(`date_from`の1日前)に経費を1件登録済み | `GET /api/dashboard/expenses`を実行 | 当該経費の金額が`monthly`・`by_category`のいずれにも含まれない(範囲外として正しく除外される) |
| TC-D05 | F-07 | 結合確認 | F-04(見積→請求変換)直後の`issue_date`がNULLの請求書が売上集計から除外される | 見積書を作成しF-04変換APIで請求書化(`issue_date`未設定のまま)済み | `GET /api/dashboard/sales-and-payments`を実行し、`sales`合計をDB上の`invoices.total_amount`合計(`issue_date`ありのみ)と突き合わせる | `issue_date`がNULLの変換直後請求書の金額は`sales`のどの月にも計上されない |
| TC-D06 | F-07 | データ整合性 | 複数勘定科目にまたがる経費内訳の集計 | 3科目以上にまたがる経費を登録済み | `GET /api/dashboard/expenses`を実行 | `by_category`に登録した科目数分の要素が含まれ、各`total_amount`が科目ごとのDB合計と一致する。金額0円の科目は含まれない |
| TC-D07 | F-07 | 機能テスト | 損益(収支)の黒字/赤字視覚区別 | 黒字月(売上>経費)・赤字月(経費>売上)が混在するデータ | `DashboardPage.tsx`の損益区画を実ブラウザで確認 | 黒字月は緑系、赤字月は赤系で棒グラフが色分け表示される(基本設計書4.13章) |
| TC-D08 | F-07 | 機能テスト | 見積成約率の算出(変換済み・未変換混在) | 直近12ヶ月に見積書複数件(一部のみF-04変換済み)を登録済み | `GET /api/dashboard/quotes`を実行 | `conversion_rate`=(変換済み件数)/(対象期間の見積作成件数)。画面上は四捨五入した%表示 |
| TC-D09 | F-07 | 異常系/境界値 | 見積作成0件時の成約率表示(0除算回避) | 直近12ヶ月に見積書が1件も無い | `GET /api/dashboard/quotes`を実行し、`DashboardPage.tsx`の見積状況区画を確認 | APIは`conversion_rate: null`を返し、例外は発生しない。画面には「-」が表示される |
| TC-D10 | F-07 | 結合確認 | ホーム画面(SC-01)から財務ダッシュボード(SC-12)への遷移導線 | - | ホーム画面のナビゲーションカードを確認し、「財務ダッシュボード」カードをクリック | `/dashboard`へ遷移し、SC-12画面が表示される |
| TC-D11 | F-07 | 結合確認 | 区画ごとの独立したエラー表示 | - | いずれか1区画のAPI(例: 損益)のみを意図的にエラー応答させ、他3区画は正常応答のまま画面を表示 | エラーにした区画のみ「情報を取得できませんでした」が表示され、他3区画は正常にグラフ・数値が表示される(他区画の表示・取得に影響しない) |
| TC-D12 | F-07 | 機能テスト | 対象期間の固定表示(直近12ヶ月、絞り込み不可) | - | SC-12画面の対象期間表示および絞り込みUIの有無を確認 | 「対象期間: 直近12ヶ月(YYYY年M月〜YYYY年M月)※期間の絞り込みはできません」の文言が表示され、期間指定のUI(日付選択等)が存在しない |
| TC-D13 | F-07 | 静的確認 | グラフ種類(月次推移=折れ線・棒、内訳=円・棒)の実装確認 | - | `DashboardPage.tsx`の各区画で使用しているRechartsコンポーネント(`BarChart`・`PieChart`)を確認 | 月次推移(売上・入金、経費、損益、見積件数・金額)は棒グラフ、経費の勘定科目別内訳は円グラフで実装されている(詳細設計書4.8章・基本設計書4.13章の要求どおり) |
| TC-D14 | F-07 | 回帰確認 | F-07実装(インデックス追加)による既存機能(F-01/F-03/F-05)への影響有無 | - | 請求書新規作成(F-01)・見積書新規作成(F-03)・入金登録(F-05)を実機で1件ずつ実行 | いずれもイテレーション1テスト結果報告書の合格時と同じ応答(ステータスコード・レスポンス内容)が得られ、インデックス追加による副作用がない |
| TC-D15 | F-07 | 境界値 | 直近12ヶ月すべてに実績がある場合の集計 | 直近12ヶ月の全月に請求書・経費が最低1件ずつ存在 | `GET /api/dashboard/sales-and-payments`・`/expenses`・`/profit-loss`を実行 | 12ヶ月全ての`amount`が0円ではなく実データを反映する(0円補完ロジックが全月に実績がある場合も正しく機能し、意図せず0円に上書きされない) |

---

## F-08 案件・プロジェクト管理 API・データ(イテレーション3・段階1)

詳細設計書3.21章・4.9章・6.5章・7章に基づく。実機はAPI直接実行(`httpx`/`curl`)+SQLiteでのデータ突合による。いずれも実DB(`db/back_office.db`)は使用せず、実DBのコード複製+使い捨てDBで実施する。

| ID | 機能ID | 観点 | テスト内容 | 事前条件 | 手順 | 期待結果 |
|---|---|---|---|---|---|---|
| TC-J01 | F-08 | 機能テスト | 案件の新規作成(正常系) | 取引先1件 | `POST /api/projects`に`name`(前後に空白を含む)・`client_id`を送信 | 201。`name`は前後空白除去、`status`は既定`NOT_STARTED`、`due_date`はnull、`client_name`が付与される |
| TC-J02 | F-08 | 異常系 | 案件名必須(未入力・空白のみ・null・欠落) | - | `POST /api/projects`に`name`が空文字・空白のみ・null・キー欠落のリクエスト | いずれも422。`detail`は「案件名を入力してください」 |
| TC-J03 | F-08 | 異常系/境界値 | 案件名の文字数境界 | - | 100文字、101文字、前後空白付き100文字で作成 | 100文字と(空白除去後)100文字は201、101文字は422 |
| TC-J04 | F-08 | 異常系 | 取引先IDの存在チェック | - | `POST`/`PUT`で存在しない`client_id`を指定 | 422(4.9.2手順3) |
| TC-J05 | F-08 | 異常系/境界値 | 納期の形式検証・正規化 | - | 納期に`2026/09/30`・`20260930`・`2026-02-30`・`2026-9-3`・`abc`・`2026-W38-1`、空文字、`2026-12-31`を指定 | 不正形式は422(「納期は日付(YYYY-MM-DD)で入力してください」)、空文字はnullで保存、正常値はそのまま保存(4.9.2手順3-3) |
| TC-J06 | F-08 | 異常系/境界値 | 概要メモの文字数境界 | - | 1000文字、1001文字で作成 | 1000文字は201、1001文字は422 |
| TC-J07 | F-08 | 機能テスト | 一覧の並び順・絞り込み・不正パラメータ | 納期あり・なし・同一納期の案件複数、取引先・ステータスが異なる案件 | `GET /api/projects`(条件なし、`status=`、`client_id=`、両方、不正値`status=XXX`) | 納期昇順・納期未設定は末尾・同順位はID昇順。絞り込みが条件どおり。不正な`status`は422。該当なしは200で空配列(4.9.3手順1) |
| TC-J08 | F-08 | 境界値 | 納期状態(超過/接近)の判定 | 納期が今日-1日、今日、今日+7日、今日+8日、未設定、完了かつ超過、-400日の案件 | `GET /api/projects`で`due_state`を確認。超過案件を`DONE`→元のステータスへ変更して再確認 | -1日=OVERDUE、当日=UPCOMING、+7日=UPCOMING、+8日=null、未設定=null、完了=null(4.9.1)。ステータス変更で再判定される |
| TC-J09 | F-08 | 機能テスト/異常系 | ステータス変更API(`PATCH /api/projects/{id}/status`) | 案件あり | 正常な列挙値、不正値、キー欠落、存在しないID、同一値への変更 | 正常は200(`status`と`updated_at`のみ更新、他項目不変)、不正値・欠落は422、存在しないIDは404、同一値も200 |
| TC-J10 | F-08 | 機能テスト/異常系 | 案件の更新・取得・削除の存在しないID、非対応メソッド、IDの型不正 | - | `PUT`/`GET`/`DELETE /api/projects/99999`、`POST /api/projects/1`、`GET /api/projects/abc`、`GET /api/projects?client_id=x` | 存在しないIDは404、非対応メソッドは405、型不正は422。`PUT`で正常更新(取引先をnullに戻す等)ができる |
| TC-J11 | F-08 | データ整合性 | 案件詳細の集計(4.9.4) | 見積1件(5,500円)・請求1件(11,000円、入金5,000円)を紐付けた案件 | `GET /api/projects/{id}`。さらに追加入金で完済、入金削除で戻す | `summary`が見積1件/5,500円、請求1件/11,000円、入金済み5,000円、未回収6,000円。請求の入金ステータスは`PARTIALLY_PAID`、完済後は`PAID`かつ未回収0円。一覧の件数と一致 |
| TC-J12 | F-08 | 機能テスト/異常系 | 書類の案件紐付け専用API(請求書・見積書・経費) | 案件2件、請求書・見積書・経費 | `PUT /api/{invoices,quotes,expenses}/{id}/project`で紐付け、別案件への移動、解除(`null`)、存在しない案件ID、存在しない書類ID、型不正、`{}`(キー省略) | 紐付け・移動・解除は200で反映。存在しない案件は422(「指定された案件が見つかりません。画面を再読み込みしてください」)かつ現状維持、存在しない書類は404、型不正は422。キー省略はnull(解除)扱い(スキーマ既定null)。他項目(番号・金額・日付・`updated_at`以外)は変更されない |
| TC-J13 | F-08 | 機能テスト | 完了済み案件への紐付け | `DONE`の案件 | 請求書を`DONE`案件へ紐付け | 警告・制限なく200(4.9.2手順5) |
| TC-J14 | F-08 | 機能テスト/データ整合性 | 通常POST/PUTでの`project_id`(請求書・見積書・経費) | 案件あり | 各POSTに`project_id`指定/無指定/存在しないID。PUTで`project_id`を省略/明示/`null`/存在しないID | 指定は反映、無指定は従来どおりnull、存在しないIDは422。**PUTでの省略は現在値を維持せずnullに更新される**(4.9.5の設計どおり)。明示すれば設定、`null`明示で解除、存在しないIDは422で現状維持 |
| TC-J15 | F-08 | 結合確認 | F-04見積→請求変換での案件引き継ぎ | 案件付き確定見積、案件なし見積 | 各見積を`POST /api/quotes/{id}/convert-to-invoice`で変換。再変換。変換後の請求書の案件変更、見積の案件解除 | 案件付き見積は請求書の`project_id`が引き継がれ(発行日・支払期限は未設定)、案件なしはnull。再変換は409。請求書側・見積側の案件変更は互いに影響しない |
| TC-J16 | F-08 | 結合確認 | 変換直後(発行日・支払期限未設定)請求書の紐付け・通常保存 | F-04変換直後の請求書 | `PUT /api/invoices/{id}/project`で案件変更。通常の`PUT /api/invoices/{id}`(日付なし)。案件詳細の集計を確認 | 専用APIは日付未設定でも200。通常PUTは従来どおり日付必須のエラー。案件詳細の件数・合計には変換直後の請求書も含まれる(4.9.4) |
| TC-J17 | F-08 | データ整合性 | 案件削除後の書類残存・`project_id`のNULL化(4.9.6) | 請求書・見積書・経費・入金・明細が紐付く案件と、他の案件 | `DELETE /api/projects/{id}`。件数・`project_id`・`updated_at`・入金・明細を確認。再度DELETE、GET | 204。請求書・見積書・経費・入金・明細の件数は削除前と一致、`project_id`がNULL化、書類の`updated_at`は不変。再DELETE・GETは404。他案件の紐付けは影響を受けない。書類APIは従来どおり参照できる |
| TC-J18 | F-08 | データ整合性 | DBスキーマ・制約(6.5章) | マイグレーション適用済みDB | `pragma foreign_key_check`/`integrity_check`、`foreign_key_list`、インデックス一覧、CHECK制約への不正値INSERT、`projects`行の直接DELETE、取引先付き案件がある状態での取引先直接DELETE | 整合性エラーなし。3テーブルの`project_id`は`ON DELETE SET NULL`。`idx_projects_*`・`idx_{invoices,quotes,expenses}_project_id`が存在。不正な`status`はCHECKで拒否。`projects`直接DELETEでも書類が残る。案件のある取引先の削除は外部キーで拒否 |
| TC-J19 | F-08 | 異常系 | 極端な値の`project_id`(型は整数だがSQLiteの範囲外) | 請求書あり | `PUT /api/invoices/{id}/project`に`{"project_id": 2^70}` | 422または404などの入力エラー(500にならない) |
| TC-J20 | F-08 | 回帰確認 | 案件付き書類のPDF出力(帳票レイアウト不変、4.13.2) | 案件付き請求書・見積書 | `GET /api/invoices/{id}/pdf`・`/api/quotes/{id}/pdf`。テンプレートの案件参照の有無を確認 | 200(`%PDF`)で出力できる。PDFテンプレートに案件名の出力はない(帳票レイアウト変更なし) |
| TC-J21 | F-08/共通 | 異常系 | ID入力の範囲検証(不具合#2の修正確認。詳細設計書4.13.2「ID範囲検証」・8章) | 請求書・見積書・経費・案件・取引先が各1件以上 | 紐付けAPI3種(`/api/{invoices,quotes,expenses}/{id}/project`)のボディ`project_id`とパスID、全ルーターのパスID(clients・invoices・quotes・expenses・payments・projects、PDF・入金・添付・変換・ステータスの各サブパス)、一覧の`client_id`クエリ、請求書・見積書・案件・経費のPOST/PUTボディの`client_id`/`project_id`へ、2^70・2^63・0・-1・`abc`・`1.5`を送信。境界確認として2^63-1も送信 | 全て422かつ`detail`=「IDの値が正しくありません」(500にならない)。2^63-1は範囲内のため404(パス)または422「指定された案件が見つかりません…」(ボディ)/200の空一覧(クエリ) |
| TC-J22 | F-08/共通 | 異常系 | 検証エラーメッセージの日本語化(詳細設計書8.1章。既存メッセージの不変を含む) | 取引先1件以上 | 案件名101文字・欠落・空白、案件概要1001文字、取引先名101文字・欠落・空、請求書のremarks1001文字(501文字は成功)、品目名欠落・101文字、数量0・小数3桁・巨大値、単価巨大、client_id欠落、空ボディ(請求書・見積書・経費・入金・設定)、ボディなし、複数エラー、経費のメモ・支払先の文字数超過を送信し、変更前コード(`c6eab9d`)の応答と比較 | 必須欠落は「<項目名>は必須です」、文字数超過は「<項目名>は<N>文字以内で入力してください」、ボディなしは「入力内容を確認してください」。既存の日本語メッセージ(名称を入力してください、品目名を入力してください、金額は0より大きい数値で入力してください等)は変更前と同一。型不正(列挙値・日付型・JSON構文)は英語のまま残る(詳細設計書8.1章の既知の制約)ことを記録する |

---

## 起動時マイグレーション自動適用(イテレーション3・段階1)

詳細設計書4.7.3.1〜4.7.3.2・4.13.1に基づく。使い捨てDB(旧スキーマ=リビジョン`2e1e85c3b7cc`+旧データ)を用意し、`python -m app.migrate`を実行する。旧スキーマDBは、イテレーション2のコード(`git archive`で取り出した複製)+Alembic適用で作成し、取引先1・請求書2・請求明細2・入金1・見積1・経費1件を投入する。

| ID | 機能ID | 観点 | テスト内容 | 事前条件 | 手順 | 期待結果 |
|---|---|---|---|---|---|---|
| TC-MG01 | F-08 | データ整合性 | 旧スキーマDBへのマイグレーション適用 | 旧スキーマ+旧データのDB | `python -m app.migrate` | 終了コード0。`alembic_version`が`a1c3f5e7b901`。適用前後で請求書・見積書・経費・入金・明細の件数が一致し、`project_id`は全件NULL。`integrity_check`・`foreign_key_check`が問題なし。`db/back_office.db.before-a1c3f5e7b901`が作成される |
| TC-MG02 | F-08 | 機能テスト | 冪等性 | TC-MG01の直後 | `python -m app.migrate`を再実行 | 「適用済みです」と表示、終了コード0、退避コピーが増えない・変わらない |
| TC-MG03 | F-08 | 回帰確認/結合確認 | 適用前後でF-07・ホームサマリー・一覧の応答が一致(4.13.1手順6) | 旧コード(イテレーション2)+旧DBと、新コード+適用後DBを別ポートで起動 | 各サーバーへ`/api/home/summary`・ダッシュボード4本・請求書/見積書/経費一覧・取引先一覧を実行して比較 | `project_id`・`project_name`の追加項目を除き完全一致 |
| TC-MG04 | F-08 | 機能テスト | `alembic_version`が無いDB(現行構造=create_all由来)の扱い | `create_all`で作成したDB(alembic_versionなし) | `python -m app.migrate` | 構造が既知リビジョンと一致するため退避後にstamp→upgradeし、終了コード0。既存データは残る |
| TC-MG05 | F-08 | 機能テスト | `alembic_version`が無い旧リビジョン構造のDB | 旧スキーマDBから`alembic_version`表を除いたもの | `python -m app.migrate` | 退避後に旧リビジョンへstamp→upgradeし、最新化される。データ件数が一致 |
| TC-MG06 | F-08 | 異常系 | `alembic_version`が無く構造が既知の版と一致しないDB | 旧スキーマDBから`alembic_version`を除き、`clients`に未知の列を追加 | `python -m app.migrate` | 終了コード1、専用案内(「更新履歴(alembic_version)がなく、構造も既知の版と一致しないため、自動更新を行いませんでした。データベースは変更していません。」)。DBファイルが一切変更されない(ハッシュ一致)。退避コピーも作られない |
| TC-MG07 | F-08 | 異常系 | 適用(upgrade)に失敗する場合 | `alembic_version`=旧リビジョンだが、途中まで異なる`projects`表(列不足)が存在するDB | `python -m app.migrate` | 終了コード1、「適用前の退避コピー(パス)から復元できます」の案内。標準出力に詳細。退避コピーは作成済み。`invoices`に`project_id`列は追加されていない |
| TC-MG08 | F-08 | 異常系 | 破損DBファイル | DBファイルが不正なバイナリ | `python -m app.migrate` | 終了コード1、「データベースファイルは退避コピー前のため変更されていません。」の案内 |
| TC-MG09 | F-08 | 機能テスト | DBファイルが存在しない場合 | DBなし | `python -m app.migrate` | 新規作成して最新まで適用(退避コピーなし)。`company_profile`初期レコードが存在する |
| TC-MG10 | F-08 | 機能テスト | 既存の退避コピーを上書きしない | 同名の退避コピー(内容: 目印文字列)が存在する状態で未適用DBを用意 | `python -m app.migrate` | 適用は行われ、既存の退避コピーの内容は変更されない |
| TC-MG11 | F-08 | 結合確認 | マイグレーション未適用のまま新コードが`create_all()`を実行した後の適用(開発時の直接起動の想定、4.13.1-5) | 旧スキーマDB | 新コードの`app.main`をインポート(`create_all`により`projects`表のみ作成される)→`python -m app.migrate` | 適用が成功し、`invoices`等に`project_id`列が追加される。整合性エラーなし |

---

## 起動.app(launch.sh)の二重起動・失敗経路(イテレーション3・段階1)

詳細設計書4.7.3〜4.7.5に基づく。`launch.sh`はスクリプト内で`127.0.0.1:8000`を固定で使うため、使い捨て環境(コード複製+使い捨てDB、`.venv`はシンボリックリンク)を用意し、ポート8000で実施する(実DBは対象外)。`open`・`osascript`はPATH先頭のスタブ(呼び出しをログに記録)へ差し替えてダイアログ・ブラウザ起動の有無を確認する。

| ID | 機能ID | 観点 | テスト内容 | 事前条件 | 手順 | 期待結果 |
|---|---|---|---|---|---|---|
| TC-LN01 | F-08 | 機能テスト | 通常起動(マイグレーション自動適用→Uvicorn起動→ブラウザ表示) | 旧スキーマDB、未起動 | `bash scripts/launch.sh` | 終了コード0。マイグレーション適用・退避コピー作成後にUvicornが起動し、ヘルスチェック200、`open`が1回呼ばれる。`GET /api/projects`が200 |
| TC-LN02 | F-08 | 機能テスト | 起動済みでの再実行 | TC-LN01の状態 | `bash scripts/launch.sh`を再実行 | マイグレーション・Uvicorn起動は行わず、`open`のみ実行して終了コード0 |
| TC-LN03 | F-08 | 結合確認 | 3プロセス同時の二重起動 | 旧スキーマDB、未起動 | `launch.sh`を3つ同時に起動 | Uvicornは1つだけ、マイグレーションは1回のみ(退避コピー1つ)、3つとも終了コード0で`open`が3回(待機側はログに「待機のみ行います」) |
| TC-LN04 | F-08 | 異常系 | マイグレーション失敗時(破損DB)の通知と中止 | 不正なバイナリのDBファイル | `bash scripts/launch.sh` | 終了コード1。ダイアログ(スタブ)に「データベースの更新に失敗したため起動を中止しました。<要約> 詳細はlogs/uvicorn.logを確認してください。」。Uvicornは起動せず`open`も呼ばれない。詳細は`logs/uvicorn.log`へ記録。ロック(`run/launch.lock`)が解放されている |
| TC-LN05 | F-08 | 異常系 | 構造不一致・`alembic_version`なしDBでの通知と中止 | TC-MG06のDB | `bash scripts/launch.sh` | 終了コード1、ダイアログに専用案内(DBを変更しなかった旨)、Uvicornは起動しない |
| TC-LN06 | F-08 | 異常系 | 古いロック(PID=存在しないプロセス)の自動回復 | `run/launch.lock/pid`に存在しないPID | `bash scripts/launch.sh` | ログに「古い起動ロックを検知したため解除します」。起動に成功し終了コード0 |
| TC-LN07 | F-08 | 異常系 | 生存プロセスの新しいロックがある場合の待機とタイムアウト | ロック内PIDが生存プロセス、更新時刻が新しい | `LAUNCH_HEALTH_MAX_ATTEMPTS=6 LAUNCH_HEALTH_INTERVAL=0.2 bash scripts/launch.sh` | マイグレーション・起動は行わず待機し、タイムアウトで「別の起動処理が完了しませんでした。logs/uvicorn.logを確認してください」を通知、終了コード1。他者のロックは解放されない |
| TC-LN08 | F-08 | 境界値 | 生存PIDでもロックが期限(`LAUNCH_LOCK_STALE_SECONDS`)を超えた場合は古いロック扱い | TC-LN07と同じロックを期限1秒に設定して2秒待つ | `LAUNCH_LOCK_STALE_SECONDS=1 bash scripts/launch.sh` | 古いロックとして解除され、起動に成功 |
| TC-LN09 | F-08 | 境界値 | PID未記入のロック(猶予後に古いと判定) | `run/launch.lock/`のみ(pidなし) | `bash scripts/launch.sh` | 猶予(既定1秒)後に「古い起動ロック(PID: 不明)」として解除し起動 |
| TC-LN10 | F-08 | 異常系 | Uvicornが起動できない場合のタイムアウト通知 | 使い捨て環境の`.venv/bin/uvicorn`を即異常終了するスタブに差し替え | `LAUNCH_HEALTH_MAX_ATTEMPTS=6 LAUNCH_HEALTH_INTERVAL=0.2 bash scripts/launch.sh` | 「起動に失敗しました。logs/uvicorn.logを確認してください」を通知し、`open`は呼ばれず、終了コード1 |
| TC-LN11 | F-08 | 機能テスト | `stop.sh`による終了(起動中・未起動) | 起動中の使い捨てサーバー/未起動 | `bash scripts/stop.sh` | 起動中: Uvicornが停止し「システムを終了しました」通知。未起動: 「システムは起動していません」通知で終了コード0 |
| TC-LN12 | F-08 | 機能テスト | `起動.app`・`終了.app`のFinderからのダブルクリック起動(GUI) | 実機のFinder | ダブルクリック操作 | ブラウザが自動で開く・終了通知が出る。GUI操作を要するため、ヘッドレス環境では実施できない場合は「未実施」とし、代替として`起動.app`のランチャーが`launch.sh`を呼ぶ構成であることを静的確認する |
| TC-LN13 | F-08 | 機能テスト | Finder起動時のUTF-8ロケール設定・長い日本語ダイアログの表示(README要確認事項#12) | 実機のFinder起動 | 意図的に失敗させ、日本語ダイアログの表示崩れを確認 | 日本語が途中で切れない。Finder起動(GUI)が必要なため、実施できない場合は「未実施」とし、README要確認事項#12として管理する |

---

## 画面(SC-01〜05・07・13〜15、イテレーション3・段階1)

詳細設計書3.13〜3.15・3.21章に基づく。本番相当構成(`backend/static`のビルド成果物をUvicornから配信、使い捨てDB)に、Playwright(Python、Chromium、ヘッドレス)で接続して実ブラウザ操作を行う。カンバンのドラッグ操作は`page.mouse`による実マウス操作で行う。

| ID | 機能ID | 観点 | テスト内容 | 事前条件 | 手順 | 期待結果 |
|---|---|---|---|---|---|---|
| TC-UI01 | F-08 | 結合確認 | SC-01「案件管理」カードからSC-14への遷移 | - | ホームで「案件管理」カードをクリック | `/projects/kanban`へ遷移し、JSエラーが発生しない |
| TC-UI02 | F-08 | 機能テスト | SC-13 案件一覧の表示項目・納期状態タグ・行クリック | 超過・接近・通常の案件がある | `/projects`を表示し、行をクリック | 案件名・取引先名・ステータス・納期・紐付け件数が表示され、超過は「超過」、接近は「接近」のタグ。行クリックでSC-15へ遷移 |
| TC-UI03 | F-08 | 機能テスト | SC-13 検索条件(ステータス・取引先) | ステータス・取引先の異なる案件 | ステータスで「完了」、取引先で「別取引先」を選択 | 条件に一致する案件のみ表示される |
| TC-UI04 | F-08 | 機能テスト | SC-14 カンバンの列構成・件数・超過強調 | 各ステータスの案件 | `/projects/kanban`を表示 | 未着手・進行中・確認待ち・完了の4列と各列の件数、超過カードの強調(赤系の枠・背景)、納期タグ |
| TC-UI05 | F-08 | 機能テスト | SC-14 ドラッグ&ドロップでのステータス変更 | 未着手の案件 | カードを別の列へマウスでドラッグ&ドロップ | 列が移動し、サーバー側のステータスが更新される(ドラッグ操作で詳細画面へ遷移しない) |
| TC-UI06 | F-08 | 境界値 | SC-14 同一列内ドロップは何もしない | 未着手の案件 | カードを同じ列内でドロップ | `PATCH`が呼ばれない |
| TC-UI07 | F-08 | 機能テスト | SC-14 「ステータス変更」メニュー | 案件あり | カード内のセレクトでステータスを選択 | ステータスが更新される |
| TC-UI08 | F-08 | 異常系 | SC-14 ステータス変更API失敗時のロールバック | `PATCH`を500応答に差し替え(`page.route`) | カードを別列へドラッグ | 元の列へ戻り、「ステータスを変更できませんでした」が表示される。サーバー値は不変 |
| TC-UI09 | F-08 | 機能テスト | SC-14 カードクリックで詳細へ遷移 | 案件あり | カードをクリック | SC-15(`/projects/{id}`)へ遷移 |
| TC-UI10 | F-08 | 機能テスト | SC-14 取引先での絞り込み | 取引先あり/なしの案件 | 取引先を選択 | 該当取引先の案件のみ表示 |
| TC-UI11 | F-08 | 機能テスト/異常系 | SC-15 新規作成(入力エラー・保存・納期超過表示) | - | `/projects/new`で案件名未入力・空白のみで保存、有効な案件名+過去日の納期で保存 | 未入力・空白は「案件名を入力してください」。保存後にSC-15(`/projects/{id}`)へ遷移し「超過」表示。新規では「削除」ボタンなし |
| TC-UI12 | F-08 | 機能テスト | SC-15 請求書の紐付けダイアログ・解除・集計サマリー | 別案件に紐付いた請求書がある | 「請求書を紐付ける」→請求書を選択→確定。「紐付けを解除」 | 別案件の書類選択時に「案件(案件名)から移動します。よろしいですか?」を表示。確定後に紐付き請求書表・集計サマリー(件数・金額)へ反映。解除で`project_id`がnullになる |
| TC-UI13 | F-08 | 機能テスト | SC-15 案件の削除(確認ダイアログ) | 請求書が紐付く案件 | 「削除」→キャンセル、再度「削除」→「削除する」 | 確認文言(「紐付いている見積書・請求書・経費は削除されず、「案件なし」に戻ります」)を表示。キャンセルで案件が残る。実行後は一覧へ遷移し、請求書は残って`project_id`がnull |
| TC-UI14 | F-08 | 機能テスト | SC-03 案件欄(通常保存での案件の維持・変更・解除) | 案件付き請求書 | 編集画面で保存、案件を別案件に変更して保存、「(案件なし)」にして保存 | 保存リクエストに現在の`project_id`が含まれ案件が外れない。変更・解除が反映される |
| TC-UI15 | F-01/F-03/F-08 | 回帰確認/結合確認 | 既存の請求書・見積書の編集画面を開いた直後に「保存」できること(4.13.2「編集時に現在値を送る」の前提) | 既存の請求書・見積書(品目あり) | 詳細画面を開き、何も変更せずに「保存」ボタンの活性状態を確認・クリック | 保存ボタンが活性で、クリックで更新される |
| TC-UI16 | F-04/F-08 | 結合確認 | SC-05→SC-03: 変換での案件引き継ぎ、変換直後の案件即時保存 | 案件付き確定見積 | 「請求書へ変換」→遷移先の請求書詳細で案件欄・注意表示を確認→案件欄を別案件に変更 | 変換元の案件が初期選択され、「発行日・支払期限が未設定」の注意が表示される。案件変更が`PUT /api/invoices/{id}/project`で即時保存され、見積書の案件は不変 |
| TC-UI17 | F-02/F-08 | 機能テスト | SC-07 経費の案件欄(新規保存・編集保存での維持) | 案件あり | 経費登録で案件を選択して保存、その経費を開き変更せず保存 | 案件付きで保存され、編集保存後も案件が維持される |
| TC-UI18 | F-01/F-03/F-08 | 機能テスト | SC-02・SC-04の「案件」列 | 案件付き・案件なしの書類 | 各一覧を表示 | 「案件」列に案件名(なしは空欄)が表示され、行が減らない |
| TC-UI19 | F-01/F-08 | 回帰確認 | SC-03 新規請求書を案件なしで作成 | - | 取引先・日付・品目を入力して保存 | 保存でき、`project_id`はnull、合計金額が正しい |
| TC-UI20 | F-01/F-03/F-04/F-08 | 回帰確認/結合確認 | 既存書類を開いて保存できること・数量の桁が壊れないこと(不具合#1の修正確認。TC-UI15の再確認、詳細設計書3.3章(注2)・4.13.2「数量の文字列正規化」) | 品目あり(数量2.50・1.00・12345678.99・0.01等)の請求書・見積書、案件付き・案件なし、F-04変換直後(発行日NULL)の請求書 | 詳細画面を開いて保存ボタンの活性を確認→数量・単価を変えずに保存→API再取得で数量・単価・金額・合計・`project_id`を比較→再度開いて保存ボタンの活性を確認。案件を別案件へ変更・「(案件なし)」へ解除して保存(TC-UI14相当、数量欄の回避操作なし) | 開いた直後から保存ボタンが活性。保存が成功(PUT 200)し数量が`2.50`・`1.00`等の小数桁のまま、金額・`project_id`が不変。保存後の再取得でも活性。案件の変更・解除も回避操作なしで反映される |

---

## 回帰確認(4.13.2チェックリスト、イテレーション3・段階1)

旧コード(イテレーション2、`git archive`で取り出した複製+旧DB、ポート8001)と新コード(マイグレーション適用済みの旧DBのコピー、ポート8002)へ、同一の操作列(取引先・請求書・見積書・経費・入金の作成/更新/削除、変換、PDF、一覧、ホーム・ダッシュボード4本)を実行し、応答(ステータス・JSON)を比較する。`created_at`・`updated_at`および追加項目(`project_id`・`project_name`)は比較から除外する。

| ID | 機能ID | 観点 | テスト内容 | 事前条件 | 手順 | 期待結果 |
|---|---|---|---|---|---|---|
| TC-RG01 | F-01 | 回帰確認 | 請求書の作成・更新・一覧・詳細・入力エラー(`project_id`なし) | 旧DBのコピー | 同一操作列を旧/新コードへ実行し比較 | ステータスコード・金額・エラーメッセージが一致(追加項目を除く) |
| TC-RG02 | F-03 | 回帰確認 | 見積書の作成・更新・一覧(`project_id`なし) | 同上 | 同上 | 一致 |
| TC-RG03 | F-04 | 回帰確認 | 見積→請求変換、再変換409、存在しない見積 | 同上 | 同上 | 一致(409の挙動が従来どおり) |
| TC-RG04 | F-02 | 回帰確認 | 経費の作成・更新・削除・一覧・期間指定エラー・集計 | 同上 | 同上 | 一致 |
| TC-RG05 | F-05・F-06 | 回帰確認 | 入金登録・入金超過エラー・入金ステータス・支払期限超過・`GET /api/home/summary` | 同上 | 同上 | 一致 |
| TC-RG06 | F-07 | 回帰確認 | ダッシュボード4本(共通集計サービスへの切り出し後) | 同上 | 同上 | 一致 |
| TC-RG07 | F-01・F-03 | 回帰確認 | 請求書・見積書のPDF出力(案件なし) | 同上 | PDF取得 | 200、`%PDF`、旧/新でサイズが一致 |
| TC-RG08 | F-01・F-03 | 回帰確認 | 一覧(SC-02・SC-04)の件数・並びが案件JOINで変わらない | 案件付き・なしの書類 | 一覧APIの件数をDB件数と比較 | 件数が一致(LEFT JOINで行が減らない) |
| TC-RG09 | 共通 | 回帰確認 | 取引先の削除(現行にAPIなし)とDB制約 | - | `DELETE /api/clients/{id}`、案件のある取引先のDB直接DELETE | APIは存在しない(404/405)。DB直接削除は外部キーで拒否(詳細設計書4.13.2・README差異#10) |
| TC-RG10 | 全機能 | 回帰確認 | developerのユニットテストの再実行結果の記録 | 依存インストール済み | `pytest -q`、`npx vitest run`を実行 | pytest 213件・vitest 52件が全件成功(README.md 5章と一致) |
| TC-RG11 | 全機能 | 回帰確認 | 修正後コードと変更前コード(`c6eab9d`)の応答比較、およびpytest・vitest・`tsc -b`の再実行(F-01〜F-07・ダッシュボード・ホームサマリー・PDF) | 同一の初期データを持つ2つの使い捨て環境 | 前回(TC-RG01〜RG09)と同じ操作列を両環境に実行し応答を比較。pytest・vitest・`tsc -b`を実行 | 応答が追加項目(`project_id`・`project_name`)を除いて完全一致し、既存の日本語メッセージが不変。pytest 271件・vitest 58件が全件成功、`tsc -b`エラーなし |

---

## 変更履歴

| 版数 | 日付 | 内容 |
|---|---|---|
| 1.0 | 2026-09-16 | 初版作成。共通機能(取引先マスタ6件・システム設定4件・起動終了5件)、F-06 3件、F-03 10件、F-04 6件、F-01 7件、F-05 13件、F-02 11件、異常系共通2件、計67件のテストケースを策定。 |
| 2.0 | 2026-09-17 | イテレーション2分を追加。F-07(財務ダッシュボード、SC-12)のテストケース15件(TC-D01〜TC-D15)を新規策定。0件時の0円/0件補完、読み取り専用(405)、一部月のみ実績、期間境界値、F-04変換直後の除外、複数勘定科目内訳、損益の黒字/赤字視覚化、見積成約率(0除算回避を含む)、ホーム画面からの導線、区画別エラー分離、対象期間固定表示、グラフ種類、既存機能への回帰確認、全月実績ありの境界値を網羅。イテレーション1分(TC-C〜TC-N、67件)は内容に変更がないため据え置き。累計82件。 |
| 3.0-stage1 | 2026-09-19 | イテレーション3・段階1(F-08)分を追加。F-08 API・データ20件(TC-J01〜TC-J20)、起動時マイグレーション自動適用11件(TC-MG01〜TC-MG11)、`launch.sh`の二重起動・失敗経路13件(TC-LN01〜TC-LN13)、画面19件(TC-UI01〜TC-UI19)、回帰確認(4.13.2)10件(TC-RG01〜TC-RG10)の計73件を新規策定。既存のTC-C〜TC-Dは内容変更なし。累計155件。 |
| 3.1-stage1 | 2026-09-19 | フォローアップ(テスト不具合#1・#2の修正、エラーメッセージ日本語化の再確認)としてTC-J21(ID入力の範囲検証)、TC-J22(検証エラーメッセージの日本語化)、TC-UI20(既存書類の保存・数量の桁保持)、TC-RG11(修正後コードの回帰比較)の4件を追加。詳細設計書3.4版(3.3章(注2)、4.13.2、5.5、8章・8.1章)に対応。既存のTC-J19・TC-UI15は仕様変更なし(再実施対象)。累計159件。 |
