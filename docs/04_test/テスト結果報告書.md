# 個人事業(EIAI TEC)向け事務管理システム テスト結果報告書

## 1. 文書情報

- 作成日: 2026-09-16 / 改訂日: 2026-09-19(フォローアップ)
- 版数: 3.1-stage1(イテレーション3・段階1: 案件・プロジェクト管理 F-08 追加。3.1でフォローアップ=不具合#1・#2修正の再確認を反映)
- 参照元:
  - `docs/04_test/テスト仕様書.md`(版数3.1-stage1)
  - `docs/04_test/テストケース.md`(版数3.1-stage1。フォローアップでTC-J21・J22・UI20・RG11の4件を追加。イテレーション3・段階1でTC-J・TC-MG・TC-LN・TC-UI・TC-RGの計73件を新規追加。TC-C〜TC-N、計67件はイテレーション1分で内容変更なし。TC-D01〜TC-D15、計15件をイテレーション2で新規追加)
  - `docs/02_architect/詳細設計書.md`(版数3.4)・`docs/02_architect/mockups/`(SC-01〜SC-15)
  - `docs/03_develop/レビュー結果報告書.md`(1〜7章=イテレーション1分、8章=イテレーション2分、9章=イテレーション3・段階1分〈指摘14〜29。9.9節がテスト不具合#1・#2の修正レビュー〉)
  - `README.md`(版数3.0-stage1-r3)
- 対応イテレーション: イテレーション1(初回実装、レビュー指摘対応・テスト指摘対応を含む)、イテレーション2(財務ダッシュボード F-07 / SC-12 追加)、イテレーション3・段階1(案件・プロジェクト管理 F-08 / SC-13〜15 追加、起動時マイグレーション自動適用)
- テスト実施方法(イテレーション1分、版数1.0〜1.1): 本番相当構成(`npm run build`→`uvicorn app.main:app --host 127.0.0.1 --port 8000`)でバックエンドを実際に起動し、`curl`+`jq`によるAPIシナリオテスト、`dist/起動.app`・`dist/終了.app`の実起動確認、実ブラウザ(Google Chrome)でのSPA読み込み確認、Playwright(Chromium)による実ブラウザ操作(TC-C06再確認)、および該当フロントエンド実装コードの静的確認を組み合わせて実施した(詳細は3.1〜3.10章)。
- テスト実施方法(イテレーション2分、本版2.0で追加): 同様に`npm run build`→`uvicorn`で本番相当構成を起動し、(a) `curl`による4本のダッシュボードAPI(`/api/dashboard/sales-and-payments`・`/expenses`・`/profit-loss`・`/quotes`)への直接リクエストとレスポンス内容の検証、(b) 取引先・請求書・見積書・経費・入金の実データをAPI経由で複数月にまたがり作成し、DB上の生データとダッシュボードAPIの集計結果を突き合わせる結合確認、(c) Playwright(Python、Chromium)による実ブラウザでのSC-01→SC-12遷移・4区画の描画内容・スクリーンショット確認、(d) Playwrightの`page.route`によるネットワークレベルのAPIモックを用いた区画別エラー表示分離の実機確認、を組み合わせて実施した(3.11章参照)。

---

## 2. 総合評価

### 2.1 件数サマリー

| 対応イテレーション | 合格 | 不合格 | 未実施 | 合計 |
|---|---|---|---|---|
| イテレーション1(TC-C〜TC-N、67件、内容変更なし) | 66 | 0 | 1 | 67 |
| イテレーション2(TC-D01〜TC-D15、F-07新規、15件) | 15 | 0 | 0 | 15 |
| イテレーション3・段階1(TC-J・TC-MG・TC-LN・TC-UI・TC-RG、F-08ほか、フォローアップで4件追加し77件) | 75 | 0 | 2 | 77 |
| **累計** | **156** | **0** | **3** | **159** |

(イテレーション3・段階1分の内訳は3.12章、不具合は4.5章を参照。イテレーション1分の内訳・詳細は3.1〜3.10章、イテレーション2分は3.11章を参照。イテレーション1分は今回変更・再実施していないため、版数1.1時点の結果をそのまま据え置いている)

### 2.2 総評(イテレーション1分、内容変更なし)

バックエンドAPI層(F-01〜F-06・共通機能の全29+1エンドポイント)については、税額計算(標準10%/非課税/不課税の区分別計算・端数切り捨て)、採番ルール、F-04見積→請求変換の1対1制約・独立性、F-05入金判定ロジック(ステータス算出・支払期限超過判定・入金超過時の確認フロー)、F-02添付ファイルのライフサイクル(アップロード・取得・削除連動)、エラーハンドリング(422/400/404/409の日本語メッセージ)を実機で網羅的に確認し、詳細設計書どおりに動作していることを確認した。レビュー結果報告書で対応済みとされていた重大度「高」「中」の指摘についても、422応答の単一文字列化・パストラバーサル拒否・経費削除時の添付ファイル連動削除を含め、tester自身の実機再検証により解消済みであることを確認した(重複記録はせず本報告書3.6章に集約)。

README「7. 要確認事項」#2で唯一「未確認」とされていた`起動.app`/`終了.app`のダブルクリック起動についても、macOSの`open`コマンド(Finderのダブルクリックと同じLaunch Services経由の起動)で実際に検証し、新規起動・二重起動時の冪等性・正常終了・未起動時終了のいずれも設計どおりに動作すること、実ブラウザでSPAが正しく読み込まれることを確認した。この要確認事項は解消したと判断する(4.1章参照)。

版数1.0の結合確認の過程では、**共通機能「取引先マスタ管理」(SC-10)への画面遷移導線が実装から欠落している**ことを検出していた(TC-C06、重大度: 高)。詳細設計書3.3章・3.5章およびmockup(SC-03・SC-05)は、請求書・見積書の詳細編集画面の取引先選択欄に「新規登録」インラインリンクを配置し、そこからSC-10へ遷移・保存後に呼び出し元へ復帰する動線を規定しているが、当時の実装(`InvoiceDetailPage.tsx`・`QuoteDetailPage.tsx`)にはこのリンクが存在しなかった。

この1件は、個々の画面のTDDユニットテスト・コンポーネントテストでは検出できない、複数画面にまたがる導線の結合確認(観点5)で初めて見つかったものであり、tester観点の重要性を裏付ける結果となった。

**【本フォローアップ版(版数1.1)での再確認結果】** developerがREADME.md版数1.2のとおり修正(`InvoiceDetailPage.tsx`・`QuoteDetailPage.tsx`の取引先選択欄に「新規登録」リンクを追加、`ClientMasterPage.tsx`からの復帰時に`selectedClientId`クエリパラメータで選択状態を復元)したことを受け、testerが実機(Playwright制御によるChromiumブラウザ)で再確認した結果、**合格**と判定した(3.1章TC-C06参照)。請求書詳細画面・見積書詳細画面の双方で、「新規登録」リンクからSC-10へ遷移し、新規取引先を保存後に呼び出し元画面へ選択済み状態で復帰することを確認済みである。

### 2.3 次フェーズ(納品)に進めるかの判断(イテレーション1分、内容変更なし)

版数1.0で不合格だったTC-C06(取引先マスタ画面への導線欠落、重大度: 高)は、本フォローアップ版のtester実機再確認により解消を確認した(合格)。修正は`InvoiceDetailPage.tsx`・`QuoteDetailPage.tsx`への「新規登録」リンク追加という単一機能内の限定的なフロントエンド修正であり、複数機能にまたがる設計変更やセキュリティに関わるものではなかったため、事前判断(版数1.0の2.3章)のとおりreviewerを挟まずtesterが直接実機再確認する二段階フローで対応した。

以上により、**イテレーション1分のテストケース67件のうち、合格66件・未実施1件(TC-A05、影響軽微)・不合格0件となり、不合格項目は残っていない。** 未実施のTC-A05(起動失敗時タイムアウト通知)は、意図的な起動失敗の再現に開発環境の破壊的操作を要するため実機確認を見送ったものであり、`launch.sh`のタイムアウト分岐コードの静的確認は完了済みで、影響は軽微(異常系の通知文言のみ)と判断する。イテレーション1時点で納品フェーズへ進める状態にあると判断していた。

### 2.4 総評(イテレーション2分、F-07財務ダッシュボード新規追加)

イテレーション2で新規実装されたF-07(財務ダッシュボード、SC-12)について、詳細設計書4.8章の機能ID単位でテスト項目を洗い出し(テストケース.md TC-D01〜TC-D15、15件)、本番相当構成で実際にバックエンドを起動し、`curl`による4本のAPI直接検証、実データ(取引先・請求書・見積書・経費・入金)を複数月にまたがり作成したうえでの集計結果の突合、Playwright(Chromium)による実ブラウザでのSC-01→SC-12遷移確認・スクリーンショット取得、ネットワークレベルのAPIモックによる区画別エラー表示分離の実機確認を実施した。

15件全て合格し、不合格・未実施はいずれも0件であった。重点確認事項(依頼内容1〜4)への対応状況は以下のとおり。

1. **正常系・異常系・境界値の機能ID単位での洗い出し**: データ0件(TC-D01)、期間境界値(`date_from`前日除外、TC-D04)、F-04変換直後の`issue_date`NULL除外(TC-D05)、複数勘定科目内訳(TC-D06)、見積成約率0件時のnull表示(TC-D09)、全12ヶ月実績ありの境界値(TC-D15)を含む15件を策定・実施した。
2. **実機での動作確認**: バックエンドを`npm run build`→`uvicorn`の本番相当構成で実際に起動し、`curl`で4本のAPI全てに疎通確認したうえで、取引先1件・請求書5件(うちF-04変換1件)・見積書3件・経費6件・入金1件を実際にAPI経由で登録し、ダッシュボードAPIのレスポンス値がDB上の実データと完全に一致することを確認した(3.11章参照)。境界値(期間開始日前日の経費除外)・発生主義(入金日ではなく発行日/発生日を基準)・見積成約率(1/3=33%)・赤字月の配色切り替えのいずれも設計どおり正しく動作することを、API応答とPlaywrightによる実ブラウザのスクリーンショットの双方で確認した。
3. **テスト成果物の改訂**: `docs/04_test/テスト仕様書.md`・`テストケース.md`・本報告書をイテレーション2分として改訂した(いずれも既存のイテレーション1分は保持のうえ追記)。
4. **不具合の有無**: **F-07の実機テストにおいて、不具合は1件も検出されなかった**(3.11章参照)。developerによるTDD実装(pytest 149件・Vitest 37件、README.md 5章)とは独立に、tester自身が実データ・実ブラウザ・APIレベルの結合確認を行った結果、詳細設計書4.8章の集計ロジック(発生主義・0円/0件補完・0除算回避・4区画独立)が全て設計どおりに実装されていることを裏付けた。developerへの差し戻しが必要な不具合はない。

以上により、**累計82件のうち、合格81件・不合格0件・未実施1件(TC-A05、イテレーション1から持ち越し、影響軽微)となり、不合格項目は残っていない。したがって、本イテレーションについても納品フェーズへ進める状態にあると判断する。** ただし、4章に記載の要確認事項(いずれも低リスク)は、deployerフェーズまたは次回イテレーションでの対応・確認を推奨する。

### 2.5 総評(イテレーション3・段階1、F-08 案件・プロジェクト管理)

**総合判定(フォローアップ後の更新): 不合格0件。不具合#1・#2は修正を実機で確認し解消した。納品フェーズへ進める状態(未実施はGUI起動に関するTC-A05・TC-LN12・TC-LN13のみ、4.6章★7)。** 以下は初版(版数3.0-stage1)時点の総評であり、経緯として残す。初版時点の判定: 条件付き(不合格2件・重大度「高」1件が未解消のため、納品前に対応を要する)。 F-08の中核(案件のCRUD・ステータス変更・納期状態・詳細集計・紐付け・F-04での案件引き継ぎ・削除時の`project_id`のNULL化)、起動時マイグレーション自動適用、`launch.sh`の二重起動・失敗経路は、使い捨て環境での実機確認において設計どおりに動作した。一方、画面確認で既存の請求書・見積書の編集画面に重大度「高」の不具合を1件、API入力の境界で重大度「低」の不具合を1件検出した。

実施の要点は次のとおりである(詳細は3.12章)。実DB(`db/back_office.db`)には一切触れず、現行コードの複製+使い捨てDB、および旧コード(イテレーション2)の複製+旧スキーマDBで実施した。

1. **F-08 API・データ(20件中19件合格)**: 入力検証(案件名・納期形式・文字数境界)、一覧の並び・絞り込み、納期状態の境界(-1日=超過、当日・+7日=接近、+8日=なし、完了=なし)、ステータス変更、詳細集計(入金・未回収・完済後)、専用紐付けAPI、通常PUTでの`project_id`省略時のNULL化(設計4.9.5どおり)、F-04変換での案件引き継ぎ、案件削除後の書類・入金・明細の残存、DB制約(`ON DELETE SET NULL`)を確認した。不合格は極端に大きい`project_id`での500応答(TC-J19、重大度: 低)のみ。
2. **マイグレーション(11件全て合格)**: 旧スキーマ+旧データDBへの適用で件数・金額・子データが不変、`project_id`は全件NULL、冪等、適用前後でF-07・ホームサマリー・各一覧の応答が(追加項目を除き)完全一致した。`alembic_version`無しDBは構造一致時のみstamp、不一致は無変更で停止(ハッシュ一致で確認)、失敗時は退避コピーの案内、破損DBも安全に停止した。
3. **launch.sh(13件中11件合格・2件未実施)**: 3並列起動でもUvicornは1つ・マイグレーションは1回、失敗時はダイアログ(スタブ)に要約+ログ案内のみを通知しUvicornを起動せずロックも解放、古いロック・PID未記入・期限超過ロックの回復、Uvicorn起動失敗のタイムアウト通知を確認した。`起動.app`のダブルクリックおよびFinder起動時のUTF-8ロケール/ダイアログ表示はGUI操作が必要なため未実施(README要確認事項#2・#12と同じ)。
4. **画面(19件中18件合格・1件不合格)**: SC-13〜15、SC-01〜05・07の案件関連の表示・操作を実ブラウザで確認した。**カンバンのドラッグ&ドロップはPlaywrightの実マウス操作で実施でき、列移動・API反映・同一列ドロップ無操作・API失敗時のロールバックと通知・ドラッグ時に詳細へ遷移しないことを全て確認した**(README要確認事項#7は解消)。不合格はTC-UI15(重大度: 高、4.5章の不具合#1)。
5. **回帰(4.13.2、10件全て合格)**: 旧コードと新コードへ同一の操作列を実行し、応答が追加項目(`project_id`・`project_name`)を除いて完全一致すること(F-01〜F-07、PDF)を確認した。pytest 213件・vitest 52件もtester環境で再実行し全件成功した。

**不具合#1(重大度: 高、TC-UI15)**: 既存の請求書・見積書を画面で開くと「保存」ボタンが無効のままになり、数量欄を一度編集しないと保存できない。原因はAPIが数量を文字列(`"1.00"`)で返す一方、フロントエンドの入力チェック(`validateItems`)が数値でないと判定するため。過去イテレーション(1.1版のレビュー指摘対応)に由来する既存の不具合で、F-08の変更で新たに入ったものではないが、詳細設計書4.13.2が回帰テスト対象とする「編集時に現在値の`project_id`を送る」保存操作の前提となるため、F-08の運用(案件付き書類を画面から保存する)に直結する。vitestは数量を数値でモックしているため検出できなかった。4.5章に再現手順と修正方針の参考を記載した。

以上のとおり、F-08そのもののロジック・データ整合性は良好で、起動時マイグレーションの安全性も設計どおりに確認できた。ただし不具合#1が未解消のままでは、EIAI TEC本人が既存書類を画面から編集・保存する業務(案件の付け替えを含む)に支障が出るため、納品前の修正と再確認を推奨する。

### 2.6 総評(イテレーション3・段階1 フォローアップ: 不具合#1・#2の修正確認、版数3.1-stage1)

**総合判定: 合格(不合格0件)。** developerの修正(詳細設計書3.4版、README 3.0-stage1-r4〜r7、レビュー結果報告書9.9節)を、tester自身が実機で再確認した。実DBには触れず、スクラッチパッド内の使い捨てDB・別ポート(8003〜8005)で実施し、フロントエンドは修正後のソースを`backend/static`を変更せずスクラッチパッドへビルドして配信した。

1. **不具合#1(既存書類の保存不可、TC-UI15): 解消。** 品目あり(数量2.50・1.00・12345678.99・0.01)の既存請求書・既存見積書(案件付き・案件なし)、F-04変換直後の請求書(発行日NULL、案件付き・案件なし)の計5書類+大数量の請求書1件を、Playwrightで開いた直後から保存ボタンが活性であることを確認した。数量・単価を変えずに保存(PUT 200)しても数量は`2.50`・`1.00`・`12345678.99`のまま、金額・税額・合計・案件が不変であり、保存後の再取得・再表示でも保存ボタンは活性のままだった(F-04変換直後は発行日・支払期限を入力して保存。これは従来仕様)。案件を別案件へ変更・「(案件なし)」へ解除して保存する操作も、数量欄の回避操作なしで請求書・見積書とも反映された(TC-UI14の条件付き合格の条件が解消)。
2. **不具合#2(ID範囲外の500、TC-J19): 解消。** 紐付けAPI3種のボディ`project_id`・パスID、全ルーターのパスID(サブパス含む)、`client_id`クエリ、POST/PUTボディの`client_id`/`project_id`に2^70・2^63・0・-1・`abc`・`1.5`を送信し、計約270件の確認で500はなく、対象の全てが422「IDの値が正しくありません」となった。範囲内の最大値(2^63-1)は従来どおり404・「指定された案件が見つかりません」(422)・空一覧(200)で、境界は妥当。
3. **日本語化(TC-J22): 合格。** 必須欠落(「案件名は必須です」「名称は必須です」「取引先は必須です」「発生日は必須です」等)、文字数超過(「案件名は100文字以内で入力してください」「名称は100文字以内で入力してください」「備考は1000文字以内で入力してください」ほか)、空ボディ(先頭の欠落項目を表示)、ボディなし(「入力内容を確認してください」)が日本語で返った。既存の日本語メッセージは変更前コード(`c6eab9d`)と同一。**型不正(列挙値=案件status・税区分・支払方法、JSON構文エラー)は英語のまま**であり、詳細設計書8.1章の既知の制約として記録する(不合格としない。レビュー指摘28と同一)。
4. **回帰(TC-RG11): 合格。** pytest 271件・vitest 58件が全件成功、`tsc -b`エラーなし。変更前コード(`c6eab9d`)と修正後コードへ同一の操作列(F-01〜F-07・PDF・ダッシュボード4本・ホームサマリー・不正入力)を実行し、38リクエストの応答が追加項目を除き完全一致した。主要画面(ホーム・請求書・見積書・経費・案件・ダッシュボード・取引先)はコンソールエラーなく表示された。

新たな不具合は検出しなかったが、観察事項を4.6章★13〜15に記載した。**修正確認フローの扱い**: 不具合#1は当初三段階(reviewer再レビュー挟む)を推奨し、その再レビューはレビュー結果報告書9.9節で実施済み、本フォローアップでtester再確認を完了した。不具合#2も同様に再確認を完了した。追加のreviewer再レビューは不要と考える。

---

## 3. 実施結果一覧

以下、テストケース.mdのIDに対応する実施結果を記載する。合格した項目の詳細な実施ログは省略し、実施方法の要点のみ記す。

### 3.1 共通: 取引先マスタ管理

| テストケースID | 実施結果 | 対応状況 | 備考・不合格時の詳細 |
|---|---|---|---|
| TC-C01 | 合格 | - | `POST /api/clients`、201応答・登録内容反映を確認 |
| TC-C02 | 合格 | - | 422応答、`detail`=「名称を入力してください」(単一文字列)を確認 |
| TC-C03 | 合格 | - | 郵便番号不正形式で422応答を確認 |
| TC-C04 | 合格 | - | 一覧取得200応答を確認 |
| TC-C05 | 合格 | - | 更新後の名称がDBに反映されることを確認 |
| **TC-C06** | **合格**(版数1.1で更新) | **対応済み**(二段階フロー: developer修正→tester実機再確認、reviewer再レビューは不要と判断) | 【版数1.0時点の不合格内容】`InvoiceDetailPage.tsx`・`QuoteDetailPage.tsx`の取引先選択欄に「新規登録」リンクが実装されていなかった(詳細は本報告書の旧版参照。下記に再現手順として残す)。<br>【修正内容(README.md版数1.2)】`InvoiceDetailPage.tsx`・`QuoteDetailPage.tsx`の取引先`Autocomplete`の隣に、mockup SC-03/SC-05どおり`/clients?returnTo=<現在のパス>`へ遷移する「新規登録」リンク(MUI`Link`+`RouterLink`)を追加。`ClientMasterPage.tsx`の既存の復帰ロジック(`navigate(`${returnTo}?selectedClientId=${saved.id}`)`)に対応する形で、両画面に`selectedClientId`クエリパラメータ受信時に取引先選択状態を復元し、パラメータを`replace`で除去する`useEffect`を追加。<br>【tester再確認方法】`npm run build`でフロントエンドを再ビルドし(`backend/static/`に反映)、`uvicorn app.main:app --host 127.0.0.1 --port 8000`で本番相当構成を起動。Playwright(Python、Chromium)で実ブラウザを操作し、以下を自動シナリオとして実行・確認した。<br>(1) 請求書詳細画面(`/invoices/{id}`)で「新規登録」リンクの存在(1件のみ)とリンク先(`/clients?returnTo=%2Finvoices%2F{id}`)を確認<br>(2) リンクをクリックし`/clients?returnTo=...`へ遷移することを確認<br>(3) 取引先マスタ画面で新規取引先(名称のみ)を入力し「保存」をクリック<br>(4) `/invoices/{id}?selectedClientId={新規ID}`へ復帰後、クエリパラメータが除去され`/invoices/{id}`になることを確認<br>(5) 取引先`Autocomplete`の表示値が新規登録した取引先名になっていることを確認<br>(6)〜(10) 見積書詳細画面(`/quotes/{id}`)についても(1)〜(5)と同様の手順を実施し、いずれも設計どおりに動作することを確認<br>【結果】請求書詳細画面・見積書詳細画面の両方で、上記(1)〜(5)/(6)〜(10)の全ステップが期待どおりに動作することを確認した。**合格。**<br>【旧・再現手順(版数1.0、解消済みのため参考として残す)】`frontend/src/pages/InvoiceDetailPage.tsx`・`QuoteDetailPage.tsx`にリンクが存在せず(grep`returnTo`・`新規登録`・`/clients`いずれも0件)、ブラウザのアドレスバーに`/clients`を直接入力する以外にSC-10へ到達できなかった。 |

### 3.2 共通: システム設定(発行者情報)

| テストケースID | 実施結果 | 対応状況 | 備考 |
|---|---|---|---|
| TC-S01 | 合格 | - | 200応答を確認 |
| TC-S02 | 合格 | - | 登録番号を含む更新が反映されることを確認 |
| TC-S03 | 合格 | - | 登録番号形式不正で422応答を確認 |
| TC-S04 | 合格 | - | `AppHeader.tsx`を確認し、`backTo`未指定(ホーム画面のみ)の場合に「システム設定」リンクが`/settings`へ表示されることを確認 |

### 3.3 共通: アプリケーション起動・終了

| テストケースID | 実施結果 | 対応状況 | 備考 |
|---|---|---|---|
| TC-A01 | 合格 | - | `open dist/起動.app`実行後、約11秒でヘルスチェック200を確認。`run/uvicorn.pid`に実プロセスIDが記録され、サーバーログ上で`GET /`・JSバンドル・`GET /api/home/summary`が200で記録された(実ブラウザがSPAを読み込み、APIを呼び出したことを確認) |
| TC-A02 | 合格 | - | 起動中に再度`open dist/起動.app`を実行し、`ps aux`でUvicornプロセスが1つのまま(PID不変)であることを確認 |
| TC-A03 | 合格 | - | `open dist/終了.app`実行後、対象PIDのプロセスが消滅し`run/uvicorn.pid`が削除されたことを確認。`osascript`ダイアログ(「システムを終了しました」)が実際に表示されたことをプロセス一覧(`ps aux`上の`osascript`プロセス)で確認し、確認後にダイアログプロセスを終了した |
| TC-A04 | 合格 | - | 未起動状態で`open dist/終了.app`を実行し、エラーなく処理完了(PIDファイルなしのまま)することを確認。ダイアログ表示の仕組み自体はTC-A03で実機確認済みのロジックと共通 |
| TC-A05 | 未実施 | - | 意図的な起動失敗(15秒タイムアウト)の再現には`.venv`の破壊等、開発環境に影響する操作が必要なため見送った。`backend/scripts/launch.sh`のタイムアウト分岐コードを静的確認し、設計どおりの`osascript`通知文言が実装されていることのみ確認した |

補足: `open`コマンドはFinderでのダブルクリックと同じmacOS Launch Services経由の起動であり、実機での動作確認として有効と判断する。ただしEIAI TEC本人が実際にFinder上でダブルクリック操作を行う場合との差異(アクセシビリティ権限・Gatekeeper初回警告等)までは本環境では検証できないため、初回利用時にGatekeeperの「開発元を確認できません」等の警告が出た場合は、右クリック→「開く」で回避できる旨を操作マニュアル(次フェーズ)に記載することを推奨する。

### 3.4 F-06 ホーム画面・共通UI

| テストケースID | 実施結果 | 対応状況 | 備考 |
|---|---|---|---|
| TC-H01 | 合格 | - | `unpaid_count`・`overdue_count`が`/api/invoices`の実データ集計と一致することを確認 |
| TC-H02 | 合格 | - | `HomePage.tsx`の`NAV_CARDS`(請求書→`/invoices`、経費管理→`/expenses`、見積書→`/quotes`、売掛金→`/receivables`)がmockup SC-01の4カードと一致することを確認 |
| TC-H03 | 合格 | - | `isError`時にサマリーエリアのみ「サマリー情報を取得できませんでした。」に差し替わり、`NAV_CARDS`のレンダリングには影響しないコードを確認 |

### 3.5 F-03 見積書作成

| テストケースID | 実施結果 | 対応状況 | 備考 |
|---|---|---|---|
| TC-Q01 | 合格 | - | 小計30,000円・消費税額2,500円(floor(25,000×0.10))・合計32,500円を確認(数量2.5×単価10,000の標準10%明細+数量1×単価5,000の非課税明細) |
| TC-Q02 | 合格 | - | `quote_number`が`2026-0001`形式で採番されることを確認 |
| TC-Q03 | 合格 | - | 明細0件で400応答を確認 |
| TC-Q04 | 合格 | - | 数量0で422応答・「数量は0より大きい数値で入力してください」を確認 |
| TC-Q05 | 合格 | - | 取引先未選択で422応答を確認 |
| TC-Q06 | 合格 | - | 取引先フィルタが機能することを確認 |
| TC-Q07 | 合格 | - | DRAFT→CONFIRMEDの切替を確認 |
| TC-Q08 | 合格 | - | PDF出力200応答、`%PDF`ヘッダを持つ有効なPDFファイル(1KB超)が生成されることを確認 |
| TC-Q09 | 合格 | - | `pdf_generation_service.py`・`pdf_base.html`を確認し、登録番号欄(`issuer_registration_number or ""`で未設定時空欄)、税区分別小計(標準10%/非課税/不課税、明細に存在する区分のみ表示)、「消費税額(標準10%)」ラベルによる適用税率表記が実装されていることを確認 |
| TC-Q10 | 合格 | - | `ItemsEditor.tsx`が`calculateTotals`/`calculateItemAmount`(フロントエンド版税計算ロジック)を用いてサーバー通信なしにリアルタイム表示していることをコードで確認(Vitestでの計算ロジック検証はREADME参照) |

### 3.6 F-04 見積→請求の自動変換

| テストケースID | 実施結果 | 対応状況 | 備考 |
|---|---|---|---|
| TC-CV01 | 合格 | - | 変換後の請求書で`issue_date`・`due_date`がNULL、`source_quote_id`が変換元見積書ID、合計金額32,500円が引き継がれることを確認 |
| TC-CV02 | 合格 | - | 再変換で409応答、「この見積書は既に請求書(No.XXX)に変換済みのため、再変換できません」を確認(レビュー指摘該当なし、新規確認) |
| TC-CV03 | 合格 | - | 変換後の請求書明細を変更・保存後、見積書側の合計金額(32,500円)が変化しないことを確認(独立性) |
| TC-CV04 | 合格 | - | 見積書詳細の`converted_invoice_id`が変換後請求書IDと一致することを確認 |
| TC-CV05 | 合格 | - | 存在しない見積書IDへの変換で404応答を確認 |
| TC-CV06 | 合格 | - | `InvoiceDetailPage.tsx`で`issue_date`/`due_date`が未設定の場合に「発行日・支払期限が未設定です」の警告(`Alert severity="warning"`)が表示される分岐を確認 |

### 3.7 F-01 請求書発行

| テストケースID | 実施結果 | 対応状況 | 備考 |
|---|---|---|---|
| TC-I01 | 合格 | - | 通常作成で`invoice_number`が`2026-NNNN`形式で採番されることを確認 |
| TC-I02 | 合格 | - | 発行日未入力の通常保存で400応答を確認(F-04変換直後の例外は本ケースでは非該当) |
| TC-I03 | 合格 | - | PDF出力200応答、有効なPDFバイナリを確認 |
| TC-I04 | 合格 | - | 取引先フィルタが機能することを確認 |
| TC-I05 | 合格 | - | 数量0・単価0のそれぞれで422応答と該当メッセージ(「数量は0より大きい数値で入力してください」「単価は0より大きい数値で入力してください」)を確認 |
| TC-I06 | 合格 | - | 明細0件で400応答を確認 |
| TC-I07 | 合格 | - | 明細変更後の更新で、小計4,000円・消費税額400円・合計4,400円に正しく再計算されることを確認 |

### 3.8 F-05 入金・売掛金管理

| テストケースID | 実施結果 | 対応状況 | 備考 |
|---|---|---|---|
| TC-P01 | 合格 | - | 入金前`payment_status`=`UNPAID`を確認 |
| TC-P02 | 合格 | - | 支払期限過去日付の請求書で`is_overdue`=`true`を確認 |
| TC-P03 | 合格 | - | 一部入金(1,000円/3,000円)登録201応答を確認 |
| TC-P04 | 合格 | - | 一部入金後`payment_status`=`PARTIALLY_PAID`を確認 |
| TC-P05 | 合格 | - | 入金額0円で422応答・「入金額は0より大きい数値で入力してください」を確認 |
| TC-P06 | 合格 | - | 残額超過の入金(force未指定)で409応答・「入金額の合計が請求金額を超過します。登録しますか?」を確認 |
| TC-P07 | 合格 | - | `force:true`で201応答・登録成功を確認 |
| TC-P08 | 合格 | - | 完済(超過込み)後`payment_status`=`PAID`、`is_overdue`=`false`を確認 |
| TC-P09 | 合格 | - | 入金記録一覧(2件)取得を確認 |
| TC-P10 | 合格 | - | 入金記録削除204応答を確認 |
| TC-P11 | 合格 | - | 削除後も`payments`テーブルの残存データからステータスが都度再算出されることを確認(DB非保持の派生値であることの確認) |
| TC-P12 | 合格 | - | `payment_status=UNPAID`フィルタで完済請求書が除外されることを確認 |
| TC-P13 | 合格 | - | `InvoiceListPage.tsx`(SC-02)・`ReceivableListPage.tsx`(SC-09)の双方で`is_overdue`に応じた背景色強調(`OVERDUE_BACKGROUND`)が実装されていることを確認 |

### 3.9 F-02 経費管理

| テストケースID | 実施結果 | 対応状況 | 備考 |
|---|---|---|---|
| TC-E01 | 合格 | - | 経費新規登録201応答を確認 |
| TC-E02 | 合格 | - | 金額0円で422応答を確認 |
| TC-E03 | 合格 | - | 発生日未入力で422応答を確認 |
| TC-E04 | 合格 | - | 期間From>Toで400応答を確認 |
| TC-E05 | 合格 | - | 初回実施時、テストスクリプトでURLエンコードを行わなかったために400応答となったが、これはテストスクリプト側の不備であり実装の不具合ではない。`curl -G --data-urlencode`で正しくURLエンコードして再実施し、200応答・該当科目のみ返ることを確認した |
| TC-E06 | 合格 | - | jpgファイルのアップロードで200応答、`attachment_path`設定を確認 |
| TC-E07 | 合格 | - | `.exe`ファイルのアップロードで400応答を確認 |
| TC-E08 | 合格 | - | 添付ファイル取得200応答を確認 |
| TC-E09 | 合格 | - | 集計金額(勘定科目別)が登録データの合計と一致することを確認 |
| TC-E10 | 合格 | - | 経費削除後、`data/attachments/expenses/{id}/`配下のファイル実体が削除されることを確認(レビュー指摘10の再検証を兼ねる) |
| TC-E11 | 合格 | - | `AttachmentService.validate()`に`config.MAX_ATTACHMENT_SIZE_BYTES`によるサイズ上限判定が実装されていることを確認(pytestでの検証はREADME 5.1章参照、10MB超のファイルを実際にアップロードする実機確認までは実施していない) |

### 3.10 異常系共通

| テストケースID | 実施結果 | 対応状況 | 備考 |
|---|---|---|---|
| TC-N01 | 合格 | - | 存在しない請求書取得で404応答を確認 |
| TC-N02 | 合格 | - | 存在しない見積書取得・経費取得・入金記録削除のいずれも404応答を確認 |

### 3.11 F-07 財務ダッシュボード(イテレーション2、新規)

実施環境: `npm run build`(TSCエラーなし、`../backend/static/assets/index-BVSe_p9h.js` 1,047.00 kB / gzip 318.10 kB、レビュー指摘13の数値と一致)→`uvicorn app.main:app --host 127.0.0.1 --port 8000`の本番相当構成で実施。テスト開始前、`db/back_office.db`は`clients`を含む全テーブルが0件のクリーンな状態であることをSQLiteで確認済み(4.3章の追記も参照。イテレーション1時点で残置していた取引先5件は、本テスト開始前に既に復元済みであった)。

| テストケースID | 実施結果 | 対応状況 | 備考 |
|---|---|---|---|
| TC-D01 | 合格 | - | データ0件状態で4API全て200応答。`sales`・`payments`・経費`monthly`・損益`monthly`・見積`monthly`がいずれも2025-10〜2026-09の12件配列で全月`amount`=0(見積は`count`=0・`total_amount`=0)、経費`by_category`=`[]`、見積`conversion_rate`=`null`であることを確認 |
| TC-D02 | 合格 | - | `/api/dashboard/sales-and-payments`へのPOST、`/expenses`へのDELETE、`/profit-loss`へのPUT、`/quotes`へのPOSTのいずれも405応答を確認 |
| TC-D03 | 合格 | - | 取引先1件・請求書4件(issue_date=2025-10-15/2026-01-10/2026-06-05/2026-09-17)・経費4件(2025-10-20/2026-01-15/2026-06-10/2026-09-05)をAPI経由で作成し、`GET /api/dashboard/sales-and-payments`・`/expenses`・`/profit-loss`を実行。実績のある4ヶ月は各請求書の`total_amount`・各経費の`amount`と完全一致(例: 2025-10 売上11,000円・経費3,000円・損益8,000円=11,000-3,000)、実績のない8ヶ月は0円で補完されることを確認。損益は4ヶ月全て「月次売上-月次経費」の計算式どおり(2026-01: 22,000-4,500=17,500円、2026-06: 33,000-2,000=31,000円、2026-09: 16,500-6,000=10,500円)一致 |
| TC-D04 | 合格 | - | 対象期間開始日(2025-10-01)の前日である2025-09-30付で経費9,999円を登録し、`GET /api/dashboard/expenses`を実行。`monthly`のいずれの月にも9,999円が含まれず、`by_category`の消耗品費合計も2025-10分の3,000円+2026-06分の2,000円=5,000円のみ(9,999円を含まない)であることを確認。期間境界(`date_from`前日除外)が設計どおり動作することを実データで確認した |
| TC-D05 | 合格 | - | 見積書(2025-11-01発行、5,500円)をF-04変換APIで請求書化(`issue_date`=NULLのまま)し、`GET /api/dashboard/sales-and-payments`を実行。`sales`合計は82,500円(=11,000+22,000+33,000+16,500の4件のみ)であり、変換直後請求書の5,500円はいずれの月にも計上されず、除外されていることを確認(F-04とF-07の結合確認) |
| TC-D06 | 合格 | - | 消耗品費2件(3,000円・2,000円)・旅費交通費1件(4,500円)・通信費1件(6,000円)の3科目・4件の経費を登録し、`GET /api/dashboard/expenses`を実行。`by_category`に3科目全てが含まれ、各`total_amount`(消耗品費5,000円・旅費交通費4,500円・通信費6,000円)がDB集計と一致することを確認。画面(SC-12)でも円グラフと凡例(件数・金額・パーセンテージ)が正しく表示されることをPlaywrightのスクリーンショットで確認(パーセンテージ合計100%、丸め誤差なし) |
| TC-D07 | 合格 | - | 2026-03に経費のみ2,000円(売上0円)を追加登録し、当該月の損益が-2,000円(赤字)になることをAPI(`amount: -2000`)で確認したうえで、`DashboardPage.tsx`を実ブラウザ(Playwright)で表示し、黒字月(4ヶ月)が緑色、赤字月(2026-03)が赤色の棒グラフで表示されることをスクリーンショットで視覚的に確認した |
| TC-D08 | 合格 | - | 見積書3件(2025-11/2026-03/2026-09発行)のうち1件をF-04変換し、`GET /api/dashboard/quotes`を実行。`conversion_rate`=0.3333...(1/3)を確認し、画面上は`Math.round(0.3333*100)`=「33%」と表示されることをPlaywrightのテキスト取得で確認(スクリーンショットにも「33%」表示を確認) |
| TC-D09 | 合格 | - | 直近12ヶ月に見積書が0件の状態で`GET /api/dashboard/quotes`を実行し`conversion_rate: null`を確認(TC-D01と同一データで確認)。`DashboardPage.tsx`の`conversionRateLabel`が`data.conversion_rate === null`時に「-」を返す分岐をコードで確認し、実ブラウザのスクリーンショットでも0件時に「-」が表示されることを確認 |
| TC-D10 | 合格 | - | Playwrightで`http://127.0.0.1:8000/`にアクセスし、`a[href="/dashboard"]`が1件のみ存在すること、クリックで`/dashboard`へ遷移しSC-12のタイトル「財務ダッシュボード」が表示されることを確認 |
| TC-D11 | 合格 | - | Playwrightの`page.route`で`/api/dashboard/profit-loss`のみ500応答に差し替え、他3エンドポイントは実サーバーへ素通しした状態でSC-12を表示。「情報を取得できませんでした」が損益区画のみ1箇所に表示され、他3区画(売上・入金状況の合計82,500円、経費の勘定科目別内訳、見積状況の33%)は正常に表示されることをスクリーンショットで確認(4.8.6章の区画独立性を実ブラウザレベルで裏付け) |
| TC-D12 | 合格 | - | SC-12画面に「対象期間: 直近12ヶ月(2025年10月〜2026年9月)※期間の絞り込みはできません」の文言が表示され、日付選択・期間指定等の絞り込みUIが存在しないことを確認 |
| TC-D13 | 合格 | - | `DashboardPage.tsx`を確認し、売上・入金状況/経費(月次)/損益/見積状況(件数・金額)はいずれも`BarChart`(棒グラフ)、経費の勘定科目別内訳は`PieChart`(円グラフ)で実装されていることを確認。詳細設計書4.8章・基本設計書4.13章は「月次推移は折れ線・棒グラフ」を許容しており、実装は棒グラフを選択(mockup `SC-12_financial-dashboard.html`も棒グラフ)。折れ線グラフは採用していないが、詳細設計書・mockupのいずれとも矛盾せず、指定範囲内の実装と判断した |
| TC-D14 | 合格 | - | インデックス追加対象の請求書発行(F-01、`POST /api/invoices`)・見積書作成(F-03、`POST /api/quotes`)・入金登録(F-05、`POST /api/invoices/{id}/payments`)を実機で実行(TC-D03〜TC-D08のデータ作成過程と兼ねる)。いずれも201応答、採番フォーマット(`quote_number`=`2026-NNNN`等)・金額計算(税額端数処理)がイテレーション1テスト結果報告書(3.5・3.7・3.8章)の合格時と同一の挙動であることを確認し、インデックス追加による既存機能への副作用がないことを確認した |
| TC-D15 | 合格 | - | 直近12ヶ月の全月(2025-10〜2026-09)に請求書1件・経費1件ずつ(月ごとに異なる金額)を実際にAPI経由で登録し、`GET /api/dashboard/sales-and-payments`・`/expenses`を実行。`sales`・`expenses.monthly`いずれも12ヶ月全てが0円ではなく、各月に登録した実データの金額(例: 2025-10 売上1,210円・経費550円、2026-09 売上2,420円・経費1,100円)と完全一致することを確認した。0円補完ロジック(`amounts.get(m, 0)`)が、実績のある月を誤って0円に上書きしないことを実データで確認済み。確認後、登録した請求書12件・経費12件は全てDELETE APIで削除し、テスト開始前の状態に復元した |

補足: TC-D01〜TC-D15の実機確認で使用したPythonスクリプトはPlaywright(`sync_playwright`)を用い、`page.route`によるAPIモックはネットワークレベルのインターセプトであり、バックエンドの実装コード自体は一切変更していない。テストデータは(1) TC-D01(0件確認)、(2) TC-D03〜TC-D14(一部月のみ実績、取引先1件・請求書5件・見積書3件・経費7件・入金1件)、(3) TC-D15(全12ヶ月実績、請求書12件・経費12件を追加)の3段階に分けて作成・確認し、各段階の確認後は実装済みのDELETE APIで請求書・見積書・経費・入金を全て削除した。取引先(クライアント)は削除APIが未実装のため(4.2章★2、イテレーション1から持ち越しの既知の制約)、テスト開始時に作成した検証用取引先1件(「tester検証用取引先(F-07)」)のみ削除できず残置している(4.4章参照)。テスト開始前時点で`db/back_office.db`は全テーブル0件のクリーンな状態であったこと(2.2章「テスト環境」参照)から、イテレーション1テスト時に残置していた取引先5件は、本テスト開始前に既に復元済み(0件)であったことを確認している。

### 3.12 イテレーション3・段階1(F-08 案件・プロジェクト管理ほか、新規)

実施環境: テスト仕様書2.2章のとおり。実DBには触れず、スクラッチパッド内(`/private/tmp/claude-501/-Users-nodaeiji-Projects-back-office/20db4e13-0dcf-4206-92c1-780ba88fe195/scratchpad/`)の`proj/`(現行コード複製+使い捨てDB)、`proj2/`(同・回帰比較用)、`oldcode/`(旧コード`66201df`複製+旧スキーマDB)、`old_schema.db`(旧スキーマ+旧データの原本)を使用した。フロントエンドは`backend/static`の既存ビルド成果物(最終ビルド以降に`frontend/src`の更新がないことを確認)を配信した。

#### 3.12.1 F-08 API・データ(TC-J01〜TC-J20)

| テストケースID | 実施結果 | 対応状況 | 備考 |
|---|---|---|---|
| TC-J01 | 合格 | - | 「  案件A  」→`案件A`、status=NOT_STARTED、due_date=null、client_name付与、201 |
| TC-J02 | 合格 | - | 空文字・空白のみ・null・キー欠落の全てで422。空文字・空白・nullは「案件名を入力してください」(キー欠落は422) |
| TC-J03 | 合格 | - | 100文字=201、101文字=422、前後空白付き100文字=201 |
| TC-J04 | 合格 | - | POST・PUTとも存在しない`client_id`は422 |
| TC-J05 | 合格 | - | 不正6種は422(「納期は日付(YYYY-MM-DD)で入力してください」)、空文字→null、`2026-12-31`は保存、不正なstatusも422 |
| TC-J06 | 合格 | - | 1000文字=201、1001文字=422 |
| TC-J07 | 合格 | - | 納期昇順・未設定は末尾・同順位はID昇順、status/client_id/複合の絞り込み、不正なstatus・型不正のclient_idは422、該当なしは200の`[]` |
| TC-J08 | 合格 | - | -1日=OVERDUE、当日=UPCOMING、+7日=UPCOMING、+8日=null、未設定=null、DONEの超過=null、-400日=OVERDUE。超過案件をDONEにするとnull、戻すとOVERDUEに再判定 |
| TC-J09 | 合格 | - | 200、`name`・`client_id`・`due_date`・`description`不変、`updated_at`のみ更新。不正値・キー欠落は422、存在しないIDは404、同一値も200 |
| TC-J10 | 合格 | - | PUT更新(取引先null化・ステータス・納期・概要)200、存在しないIDのPUT/GET/DELETEは404、`POST /api/projects/1`は405、`GET /api/projects/abc`は422 |
| TC-J11 | 合格 | - | 見積1件5,500円・請求1件11,000円・入金5,000円・未回収6,000円、`PARTIALLY_PAID`。追加入金で`PAID`・未回収0円。変換直後(発行日NULL)の請求書も一覧・集計に含まれる |
| TC-J12 | 合格 | - | 3種の書類で紐付け・移動・解除が反映。存在しない案件は422(「指定された案件が見つかりません。画面を再読み込みしてください」)で現状維持、存在しない書類は404、`project_id`が文字列は422。`{}`(キー省略)はnull=解除扱い(スキーマ既定値どおり。送信漏れで解除される点は4.5章の留意事項) |
| TC-J13 | 合格 | - | `DONE`案件への紐付けも200 |
| TC-J14 | 合格 | - | 請求書・見積書・経費でPOST/PUTとも確認。**PUTで`project_id`を省略するとNULL化される**(設計4.9.5どおり)、明示で設定、null明示で解除、存在しないIDは422かつ現状維持 |
| TC-J15 | 合格 | - | 案件付き見積→請求書の`project_id`が一致(発行日NULL)、案件なし→null、再変換409。請求書側の案件変更・見積側の解除は互いに影響しない |
| TC-J16 | 合格 | - | 変換直後の請求書へ専用APIで案件変更200、通常PUT(日付なし)は従来どおりエラー、案件詳細の集計に含まれる |
| TC-J17 | 合格 | - | 204。請求書・見積書・経費・入金・明細の件数が削除前後で一致、`project_id`がNULL化、`updated_at`不変、再DELETE/GETは404、他案件の紐付けは不変。変換由来の請求書を持つ案件も削除可 |
| TC-J18 | 合格 | - | `foreign_key_check`・`integrity_check`問題なし。3テーブルの`project_id`は`ON DELETE SET NULL`、`idx_projects_status/client_id/due_date`と`idx_{invoices,quotes,expenses}_project_id`が存在、不正statusはCHECKで拒否、`projects`直接DELETEでも書類が残る、案件のある取引先の直接DELETEは外部キー違反で拒否。なお`projects.client_id`は`NO ACTION`(設計書は「既定のRESTRICT」と記載。SQLiteでは削除拒否の挙動は同じで、記載どおり既定値のまま) |
| TC-J19 | 合格(フォローアップで再確認、初版は不合格) | 解消(4.5章 不具合#2) | 初版: `PUT /api/invoices/{id}/project`に2^70を送ると500。フォローアップ: 同APIに2^70・0・-1・`abc`・`1.5`を送ると全て422「IDの値が正しくありません」(3種の紐付けAPIとも)。詳細はTC-J21 |
| TC-J20 | 合格 | - | 案件付き請求書・見積書のPDFが200・`%PDF`で出力。`backend/app/templates`に案件参照はなく帳票レイアウトは不変(`pdftotext`が無いためPDF本文の目視は未実施、テンプレートの静的確認で代替) |

#### 3.12.2 起動時マイグレーション自動適用(TC-MG01〜TC-MG11)

| テストケースID | 実施結果 | 対応状況 | 備考 |
|---|---|---|---|
| TC-MG01 | 合格 | - | 旧スキーマDBへ適用。終了コード0、`alembic_version`=`a1c3f5e7b901`、`db/back_office.db.before-a1c3f5e7b901`作成。請求書2・見積書1・経費1・入金1・明細2件が不変、`project_id`は全件NULL、`integrity_check`=ok、`foreign_key_check`なし |
| TC-MG02 | 合格 | - | 再実行で「適用済みです」、終了コード0、退避コピーは増えない |
| TC-MG03 | 合格 | - | 旧コード(8001)と新コード(8002)で`home/summary`・ダッシュボード4本・請求書/見積書/経費/取引先一覧を比較し、`project_id`・`project_name`を除き完全一致(件数・金額・ステータス含む) |
| TC-MG04 | 合格 | - | `create_all`由来(alembic_versionなし・現行構造)は退避後にstamp→upgradeで`a1c3f5e7b901`。データは残存。再実行は「適用済み」 |
| TC-MG05 | 合格 | - | alembic_versionなし・旧リビジョン構造は退避後にstamp→upgradeで最新化。データ件数一致 |
| TC-MG06 | 合格 | - | 終了コード1、専用案内(「…自動更新を行いませんでした。データベースは変更していません。サポートへご連絡ください。」)を標準エラーへ出力。DBファイルのハッシュが実行前後で一致、退避コピーは作成されない |
| TC-MG07 | 合格 | - | 終了コード1、「適用前の退避コピー(パス)から復元できます。」。標準出力に`no such column: status`等の詳細。退避コピーあり、`invoices.project_id`列は未追加(ALTERは反映されていない) |
| TC-MG08 | 合格 | - | 終了コード1、「データベースファイルは退避コピー前のため変更されていません。」、詳細は標準出力 |
| TC-MG09 | 合格 | - | DBなしから新規作成して最新まで適用(退避コピーなし)、`company_profile`初期1件 |
| TC-MG10 | 合格 | - | 同名の退避コピー(内容「MARKER」)を置いた状態で適用。適用は成功し、既存の退避コピーの内容は不変。なお完了メッセージは「退避コピー: <パス>」と表示するが、この場合そのファイルは今回の適用前DBの複製ではない(設計どおり「先に取られた退避を保全」する仕様。情報として記載) |
| TC-MG11 | 合格 | - | 旧スキーマDBに新コードの`app.main`をインポート(`create_all`が`projects`表のみ作成)した後に`app.migrate`を実行しても成功(`CREATE TABLE IF NOT EXISTS`)、`project_id`列が3テーブルに追加され整合性エラーなし。開発時に`migrate`前に直接uvicornを起動した場合のリスクは限定的と確認 |

#### 3.12.3 起動.app(launch.sh)の二重起動・失敗経路(TC-LN01〜TC-LN13)

| テストケースID | 実施結果 | 対応状況 | 備考 |
|---|---|---|---|
| TC-LN01 | 合格 | - | 旧スキーマDBから約1.7秒で起動。マイグレーション適用→退避コピー→Uvicorn起動→ヘルス200→`open`1回。`GET /api/projects`=`[]` |
| TC-LN02 | 合格 | - | 起動済みで再実行すると`open`のみ実行、終了コード0、`run/`にロック残存なし |
| TC-LN03 | 合格 | - | 3つ同時起動: Uvicornプロセス1つ、マイグレーション1回(退避コピー1つ)、ログに「待機のみ行います」2回、`open`3回、全て終了コード0 |
| TC-LN04 | 合格 | - | 破損DB: 終了コード1、ダイアログ(スタブ)は「データベースの更新に失敗したため起動を中止しました。データベースのマイグレーション(更新)に失敗しました。データベースファイルは退避コピー前のため変更されていません。 詳細はlogs/uvicorn.logを確認してください。」のみ。詳細(トレース相当)は`logs/uvicorn.log`、Uvicorn未起動、`open`未呼び出し、`run/launch.lock`は解放済み |
| TC-LN05 | 合格 | - | 構造不一致: 終了コード1、専用案内(DBを変更しなかった旨)がダイアログ要約に含まれ、Uvicorn未起動 |
| TC-LN06 | 合格 | - | PID=99999(不在)のロックでログに「古い起動ロック(PID: 99999)を検知したため解除します」、起動成功、終了コード0 |
| TC-LN07 | 合格 | - | 生存プロセスのPIDを持つ新しいロック: 待機のみで起動せず、6回(約1.2秒)後に「別の起動処理が完了しませんでした。…」を通知、終了コード1。他者のロックは残る(解放されない)。マイグレーションも未実行(`alembic_version`が旧のまま) |
| TC-LN08 | 合格 | - | 同じロックを`LAUNCH_LOCK_STALE_SECONDS=1`+2秒経過で古いロックとして解除・起動成功 |
| TC-LN09 | 合格 | - | PID未記入ロックは約1秒の猶予後に「古い起動ロック(PID: 不明)」として解除・起動成功(所要約2.4秒) |
| TC-LN10 | 合格 | - | `uvicorn`が即異常終了するスタブ: 「起動に失敗しました。logs/uvicorn.logを確認してください」を通知、`open`なし、終了コード1(タイムアウトは環境変数で短縮して確認。既定15秒の待機自体は未計測)。なお失敗後も`run/uvicorn.pid`は残る(次回起動時に上書きされるため実害なし、情報) |
| TC-LN11 | 合格 | - | 起動中: Uvicorn停止・「システムを終了しました」通知・ポート解放。未起動: 「システムは起動していません」通知、終了コード0 |
| TC-LN12 | 未実施 | 該当なし(README要確認事項#2・#8・#12と同一) | `起動.app`のFinderダブルクリックはGUI操作のため実施不可。代替として`dist/起動.app/Contents/MacOS/launcher`が`backend/scripts/launch.sh`を`exec`する構成であることのみ静的確認した。実DBへの初回自動適用(`db/back_office.db.before-a1c3f5e7b901`の作成)の確認も、実DBに触れない条件のため未実施(README要確認事項#8) |
| TC-LN13 | 未実施 | 該当なし(README要確認事項#12と同一) | Finder起動時のロケール・長い日本語ダイアログの表示はGUI操作が必要なため未実施。ターミナル実行時の`launch.sh`(`LC_ALL`設定は`locale -a`から選択)の正常動作のみ確認 |

#### 3.12.4 画面(TC-UI01〜TC-UI19)

| テストケースID | 実施結果 | 対応状況 | 備考 |
|---|---|---|---|
| TC-UI01 | 合格 | - | ホームに「案件管理」カード、クリックで`/projects/kanban`へ遷移。JSエラーなし |
| TC-UI02 | 合格 | - | 列見出し・件数(見積/請求)・「超過」「接近」タグを確認。行クリックでSC-15へ遷移 |
| TC-UI03 | 合格 | - | ステータス「完了」で完了案件のみ、取引先「別取引先」で該当案件のみ表示 |
| TC-UI04 | 合格 | - | 4列(未着手・進行中・確認待ち・完了)と件数、超過カードの赤系強調、接近タグをスクリーンショットで確認 |
| TC-UI05 | 合格 | - | **ドラッグ&ドロップを`page.mouse`で実施**し、未着手→進行中へ移動、サーバー側ステータスがIN_PROGRESSに更新。ドラッグ操作でURLは変わらず(詳細へ遷移しない) |
| TC-UI06 | 合格 | - | 同一列内ドロップで`PATCH`は0回 |
| TC-UI07 | 合格 | - | カード内「ステータス変更」セレクトで確認待ちへ変更、サーバー反映 |
| TC-UI08 | 合格 | - | `PATCH`を500に差し替えてドラッグ: 元の列に戻り、「ステータスを変更できませんでした」を表示、サーバー値は不変。ルート解除後の再ドラッグは成功 |
| TC-UI09 | 合格 | - | カードクリックで`/projects/{id}`へ遷移 |
| TC-UI10 | 合格 | - | 取引先「旧取引先」選択で該当案件のみ、取引先なしの案件は非表示 |
| TC-UI11 | 合格 | - | 未入力・空白のみで「案件名を入力してください」、新規では削除ボタンなし。有効入力+過去日で保存後に`/projects/{id}`へ遷移し「超過」表示 |
| TC-UI12 | 合格 | - | 他案件の請求書を選択すると「から移動します。よろしいですか?」を表示、確定で紐付き請求書表・サマリー(請求書1件・11,000円)に反映、APIも移動済み。「紐付けを解除」でproject_id=null |
| TC-UI13 | 合格 | - | 確認文言を表示、キャンセルで案件が残る、実行で一覧へ遷移、請求書は残り`project_id`=null |
| TC-UI14 | 合格(フォローアップで条件解消) | - | 保存で案件が外れず(保存リクエストに現在の`project_id`を含む)、変更・「(案件なし)」への解除が反映された。初版では数量欄を一度編集する回避操作を挟んでいたが、フォローアップで回避操作なしに請求書・見積書とも変更・「(案件なし)」への解除が反映されることを再確認(TC-UI20) |
| TC-UI15 | 合格(フォローアップで再確認、初版は不合格) | 解消(4.5章 不具合#1) | 初版: 開いた直後に「保存」が無効。フォローアップ: 既存請求書・既存見積書・変換直後の請求書のいずれも開いた直後から保存ボタンが活性、保存後の再取得でも活性(TC-UI20) |
| TC-UI16 | 合格 | - | 変換後に請求書詳細へ遷移、案件欄に元見積の案件が初期選択、「未設定」の注意表示、案件を変更すると`PUT /api/invoices/{id}/project`が呼ばれ即時保存、見積の案件は不変。見積編集画面での保存も案件が外れない(こちらも数量欄の回避操作あり、TC-UI15参照) |
| TC-UI17 | 合格 | - | 経費登録で案件を選択して保存(案件付きで保存)、編集画面で変更せず保存しても案件維持(経費は金額が数値のため保存ボタンは活性) |
| TC-UI18 | 合格 | - | 請求書一覧・見積書一覧に「案件」列と案件名を表示 |
| TC-UI19 | 合格 | - | 案件なしの新規請求書を画面から作成(合計5,500円)、`project_id`=null |

#### 3.12.5 回帰確認(TC-RG01〜TC-RG10)

| テストケースID | 実施結果 | 対応状況 | 備考 |
|---|---|---|---|
| TC-RG01 | 合格 | - | 請求書の作成・更新・一覧・詳細・不正入力(日付なし・品目名空)のステータス・JSONが旧/新で一致(追加項目を除く) |
| TC-RG02 | 合格 | - | 見積書の作成・更新・一覧が一致 |
| TC-RG03 | 合格 | - | 変換・再変換409・存在しない見積404が一致 |
| TC-RG04 | 合格 | - | 経費の作成・更新・削除・一覧・期間逆転エラー・集計が一致 |
| TC-RG05 | 合格 | - | 入金登録・入金超過エラー・入金後の請求書ステータス・`home/summary`(操作前後)が一致 |
| TC-RG06 | 合格 | - | ダッシュボード4本が旧/新で一致 |
| TC-RG07 | 合格 | - | 請求書・見積書のPDF出力が旧/新とも200で同一サイズ |
| TC-RG08 | 合格 | - | 請求書一覧・見積書一覧の件数がDB件数と一致(案件なし含む) |
| TC-RG09 | 合格 | - | `DELETE /api/clients/1`は404/405(APIなし。README差異#10・要確認事項#10のとおり)。案件のある取引先の直接DELETEは外部キー違反で拒否 |
| TC-RG10 | 合格 | - | tester環境で再実行: pytest **213 passed**(1 warning、12.7秒)、vitest **52 passed**(9ファイル)。README.md 5章の記載と一致。ただしvitestは数量を数値でモックしているためTC-UI15の不具合を検出できていない |


#### 3.12.6 フォローアップ追加ケース(TC-J21・TC-J22・TC-UI20・TC-RG11、版数3.1-stage1)

環境: スクラッチパッド`proj3/`(修正後コード複製+使い捨てDB、ポート8003)、`head6/`(変更前コード`c6eab9d`の`git archive`、ポート8004)、`cur6/`(修正後コード複製、ポート8005)。スクリプトはスクラッチパッドの`v3_*.py`。

| テストケースID | 実施結果 | 対応状況 | 備考 |
|---|---|---|---|
| TC-J21 | 合格 | - | 紐付けAPI3種のボディ・パスID、6グループのパスID(サブパス含む)、`client_id`クエリ(請求書・見積書・案件)、POST/PUTボディ(請求書・見積書=`client_id`/`project_id`、案件=`client_id`、経費=`project_id`)へ6種の不正値(2^70、2^63、0、-1、`abc`、`1.5`)を送信。全て422「IDの値が正しくありません」、500は0件。境界: 2^63-1はパス404・ボディ422(案件が見つかりません)・クエリ200(空)。なお、当該メソッドを持たないルート(例: `GET /api/payments/{id}`)は404(ID形式によらず)を返すが、ID検証の対象外(従来どおり)。`GET /api/expenses?client_id=`は経費にclient_id条件がなく無視される(従来どおり、仕様外) |
| TC-J22 | 合格(型不正は既知の制約) | - | 上記2.6章のとおり。必須欠落・文字数超過・空ボディ・ボディなしが日本語、既存メッセージは変更前コードと同一。型不正の英語残存(案件status「Input should be 'NOT_STARTED', ...」、請求書の税区分、経費の支払方法、JSON構文「JSON decode error」)は詳細設計書8.1章の既知の制約として記録。複数エラーは先頭のみを返す(例: 取引先ID型不正と品目空で「IDの値が正しくありません」)。品目空(`items: []`)は従来どおり400「品目明細を1件以上入力してください」 |
| TC-UI20 | 合格 | - | 上記2.6章1のとおり。既存請求書・見積書・変換直後の請求書(案件あり/なし)で保存ボタン活性、数量・単価・金額・案件不変(2.50・1.00・12345678.99・0.01を確認)、保存後の再表示でも活性、案件の変更・解除も反映。ページエラー0件 |
| TC-RG11 | 合格 | - | pytest 271 passed(1 warning)、vitest 58 passed(10ファイル)、`tsc -b`エラーなし。変更前/修正後の38リクエスト応答が完全一致(追加項目の`project_id`・`project_name`・日時を除く)、ステータスも一致(pay-over 409、q-convert2 409、e-range 400など)。詳細設計書4.13.2の各行(F-01・F-02・F-04・F-05・F-06・F-07・一覧・PDF)のうちAPI比較で確認できる範囲は一致 |

---

## 4. 要確認事項・未解決事項

### 4.1 起動.app/終了.appのダブルクリック起動(README要確認事項#2)→本テストで解消

README.md「7. 要確認事項」#2に記載の「`起動.app`/`終了.app`のダブルクリック起動が未確認」は、本テスト(TC-A01〜TC-A04)により、macOSの`open`コマンド(Finderのダブルクリックと同じLaunch Services経由の起動)を用いた実機確認で解消したと判断する。設計どおりの起動・冪等性・終了・通知動作を確認済みである。ただし、Finder上での実際のマウス操作(Gatekeeperの初回警告ダイアログ等)そのものは本環境(GUIを持たない自動化環境)では検証できていないため、EIAI TEC本人が初回利用時に警告ダイアログが出た場合の対処(右クリック→「開く」)を次フェーズ(操作マニュアル)に記載することを推奨する。

| ★ | 内容 | 影響 | 確認方法 | 確認先 | 期限目安 |
|---|---|---|---|---|---|
| 1 | Finder上での実際のダブルクリック操作(Gatekeeper初回警告への対処)は未検証 | 低(`open`コマンドでの動作確認により内部処理は問題ないことを確認済み。初回起動時の警告表示のみ未検証) | EIAI TEC本人の実機でのダブルクリック確認、または操作マニュアルへの対処法記載 | deployer/EIAI TEC本人 | 納品フェーズ |

### 4.2 テスト実施中に判明した詳細設計書内の軽微な記載不整合(architectフェーズでの整理を推奨、実装への影響なし)

以下はいずれも実装コードの不具合ではなく、詳細設計書内の章同士、または詳細設計書とmockupsとの間の記載レベルの軽微な不整合である。実装はより詳細な記載(3章の画面操作定義表・7章のAPI設計表・mockups)と整合しており、そちらを正として判断した。次回architectフェーズでの詳細設計書の記載整理を推奨するが、実装修正は不要と考える。

| ★ | 内容 | 影響 | 確認方法 | 確認先 | 期限目安 |
|---|---|---|---|---|---|
| 2 | 詳細設計書5.1章のクラス設計表は`routers/clients.py`の対象エンドポイントに「`GET/PUT/DELETE /api/clients/{id}`」と記載しているが、3.10章(SC-10画面操作定義表)・7章(API設計表)のいずれにも取引先削除の操作・APIは定義されておらず、実装(`routers/clients.py`)にも存在しない | 低(削除不要という仕様であれば実装は正しい。5.1章の記載のみの誤りと考えられる) | 3.10章・7章・mockup(SC-10に削除ボタンなし)との整合を確認 | architect(次回イテレーション) | 任意 |
| 3 | 詳細設計書3.1章(SC-01操作定義表)は「各機能カードのクリック...3.2/3.6/3.9/3.10」と記載しているが、mockup SC-01の実際のカード構成は請求書(3.2)・経費管理(3.6)・見積書(3.4)・売掛金(3.9)であり、3.10(取引先マスタ)は含まれず3.4(見積書)が含まれる。実装はmockupと一致している | 低(実装はmockupどおりで問題なし。3.1章の記載のみの誤りと考えられる) | mockup SC-01と3.1章の突き合わせ | architect(次回イテレーション) | 任意 |
| 4 | 詳細設計書3.3章の操作定義表で「取引先「新規登録」リンク」の遷移先節番号が「3.11(取引先マスタ管理)」と記載されているが、取引先マスタ管理は3.10章であり、3.11章はシステム設定(発行者情報)である | 低(内容自体はSC-10への遷移を指しており意図は明確。節番号の誤記のみ) | 3.10章・3.11章の見出しとの突き合わせ | architect(次回イテレーション) | 任意 |

### 4.3 テストで発生した残置データ(db/back_office.db)

テスト実施にあたり、`db/back_office.db`に取引先マスタのテストデータ5件(版数1.0時点の3件「テスト商事株式会社(更新後)」「第二テスト商事」「追加テスト取引先」に加え、本フォローアップ版(TC-C06再確認)でPlaywrightシナリオ中に作成した2件「Playwrightテスト TC-C06請求書用取引先」「Playwrightテスト TC-C06見積書用取引先」)、およびシステム設定(発行者情報)にテスト値(氏名「テスト太郎」等)が残っている。請求書・見積書・経費・入金記録は、実装済みのDELETE APIを用いてテスト終了後に全て削除しテスト前の状態(0件)に復元済みである(本フォローアップ版でTC-C06再確認のために作成した請求書1件・見積書1件も、確認後に`DELETE /api/invoices/1`・`DELETE /api/quotes/1`で削除しテスト前の状態に復元済み)。取引先マスタについては削除APIが実装されていないため(4.2章★2参照)、APIでは削除できず、また直接SQLでのDB書き換えは本環境の破壊的操作に対する安全機構により実行がブロックされたため、テスター側では復元できなかった。

| ★ | 内容 | 影響 | 確認方法 | 確認先 | 期限目安 |
|---|---|---|---|---|---|
| 5 | `db/back_office.db`にtester作成のテスト用取引先5件(版数1.0時点3件+版数1.1フォローアップ2件)・システム設定のテスト値が残置されている(テスト開始時点でDBは全テーブル空、company_profileのみ初期空レコード1件だったことを確認済み) | 低(本番データではなくテストデータのみ。個人事業主が実運用を開始する前に整理すれば実害なし) | (a) `db/back_office.db`を削除し`alembic upgrade head`で再作成するか、(b) SC-10画面で該当5件の取引先名称を実データに書き換えて流用するか、(c) 明示的な許可のもとtester/developerが直接SQLで削除する、のいずれかで対応可能 | EIAI TEC本人/deployer | 納品フェーズまで |

【本版(版数2.0)での確認】イテレーション2テスト開始前に`db/back_office.db`を確認したところ、`clients`テーブルを含む全テーブルが0件のクリーンな状態であった(README.md版数2.0の要確認事項★6のとおり、developerがイテレーション2の実装確認後にDBを復元済みであったため)。したがって★5の残置データ(取引先5件)は本版時点では既に解消しており、EIAI TEC本人・deployerによる対応は不要となった。ただし、本版のF-07テストで新たに1件の残置データが発生している(4.4章参照)。

### 4.4 F-07テストで発生した残置データ(イテレーション2、新規)

TC-D01〜TC-D15の実施にあたり、`db/back_office.db`に検証用取引先を1件作成した(「tester検証用取引先(F-07)」)。関連して作成した請求書(合計17件)・見積書(3件)・経費(合計18件)・入金記録(1件)は、いずれも実装済みのDELETE APIを用いてテスト終了後に全て削除し、テスト開始前の状態(0件)に復元済みである。取引先マスタのみ、4.2章★2のとおり削除APIが実装されていないため復元できなかった。

| ★ | 内容 | 影響 | 確認方法 | 確認先 | 期限目安 |
|---|---|---|---|---|---|
| 6 | `db/back_office.db`にtester作成の検証用取引先1件(「tester検証用取引先(F-07)」)が残置されている(取引先マスタ以外は請求書・見積書・経費・入金全てDELETE APIで削除済み、0件に復元済み) | 低(本番データではなくテストデータのみ。個人事業主が実運用を開始する前に整理すれば実害なし。4.2章★2で報告済みの構造的な制約(取引先削除APIの未実装)に起因するものであり、新規の実装不具合ではない) | (a) `db/back_office.db`を削除し`alembic upgrade head`で再作成するか、(b) SC-10画面で当該取引先名称を実データに書き換えて流用するか、(c) 明示的な許可のもとtester/developerが直接SQLで削除する、のいずれかで対応可能 | EIAI TEC本人/deployer | 納品フェーズまで |


### 4.5 イテレーション3・段階1のテストで検出した不具合(新規)

不具合はコードの修正を行わず、指摘のみとする(修正はdeveloperの担当)。**フォローアップ(版数3.1-stage1)で不具合#1・#2とも修正を実機で再確認し、解消した。残存する不具合は0件。**以下の再現手順・所見は初版の記録として残す。

#### 不具合#1(重大度: 高): 既存の請求書・見積書を画面で開いた直後は「保存」できない(TC-UI15。TC-UI14・TC-UI16は回避操作を挟んで実施)【解消済み・フォローアップで確認】

| 項目 | 内容 |
|---|---|
| 影響機能 | F-01(SC-03)・F-03(SC-05)・F-04(変換直後の請求書、SC-03。同一の入力欄のため同様と推定、変換直後の保存操作そのものは未検証)・F-08(案件付き書類の画面保存、詳細設計書4.13.2の「編集時に現在値を送る」画面テスト) |
| 起因 | 今回のF-08の変更ではなく、イテレーション1(README版数1.1、レビュー指摘1対応の「保存ボタン活性制御」追加)に由来する既存の不具合。旧コード(イテレーション2)のAPIも数量を文字列で返しており、今回の確認で初めて画面から検出された |
| 再現手順 | (1) 品目を含む請求書(または見積書)をAPIで作成する。(2) ブラウザで`/invoices/{id}`(または`/quotes/{id}`)を開く。(3) 何も変更せずに「保存」ボタンの状態を確認する→**disabled**(押せない)。(4) 品目の数量欄を一度編集(例: `1.00`を`1`に入力し直す)すると有効になる。(5) その状態で保存すると、保存後に再取得されて再度disabledに戻る |
| 期待結果 | 開いた直後から保存ボタンが活性であること(詳細設計書3.3・3.5章) |
| 原因の所在(参考) | `GET /api/invoices/{id}`は数量を文字列(`"quantity": "1.00"`、PydanticのDecimal)で返し、`frontend/src/components/ItemsEditor.tsx`の`validateItems`が`Number.isFinite(item.quantity)`で判定するため文字列は`false`となり「数量は0より大きい数値で入力してください」扱いで保存ボタンが無効になる(`InvoiceDetailPage.tsx`の`disabled={... || !!itemsErrorMessage}`)。vitestは数量を数値でモックしているため検出できなかった |
| 回避策 | 数量欄を一度入力し直してから保存する(業務上の運用回避は可能だが、利用者に気付きにくい) |
| 修正確認フロー | **三段階(developer修正→reviewer再レビュー→tester再確認)を推奨**。修正がフロントエンド(数量の数値化)かAPI契約(数量の返却型)かで設計整合性に関わり、F-01・F-03・F-04・F-08の複数画面にまたがるため |
| 対応状況 | **解消(フォローアップ版3.1-stage1)**。修正は`frontend/src/utils/items.ts`の`toItemInputs`(取得した数量・単価を数値へ正規化、API型は不変。詳細設計書3.3章(注2))。reviewerの再レビュー(レビュー結果報告書9.9節)を経て、testerがTC-UI15・TC-UI20で実機再確認し合格。数量は小数桁(`2.50`・`1.00`・`12345678.99`)が保存後も崩れないことを確認した |

#### 不具合#2(重大度: 低): 極端に大きい`project_id`で500応答(TC-J19)【解消済み・フォローアップで確認】

| 項目 | 内容 |
|---|---|
| 影響機能 | F-08(書類の案件紐付け専用API。通常のPOST/PUTの`project_id`、`/api/projects/{id}`等のパスのIDも同様の可能性があるが未確認) |
| 再現手順 | 請求書1件を作成し、`curl -X PUT http://127.0.0.1:8000/api/invoices/1/project -H 'Content-Type: application/json' -d '{"project_id": 1180591620717411303424}'`を実行する |
| 期待結果 | 入力エラー(422等)または「案件が見つかりません」(422/404) |
| 実際の結果 | 500(`{"detail":"処理に失敗しました。しばらくしてから再度お試しください"}`)。SQLiteの整数範囲(64ビット)を超える値でDB側がオーバーフローするものと推定 |
| 影響度 | 画面からは到達しない(案件IDは一覧から選ぶため)。不正なAPI直接呼び出しのみ |
| 修正確認フロー | 二段階(developer修正→tester直接再確認)で足りる。単一機能(案件ID入力の上限検証)に閉じた軽微な修正のため(実際の修正は全ルーターの共通型`EntityId`適用に広がり、reviewer再レビュー〈9.9節〉を経た上でtester再確認を実施) |
| 対応状況 | **解消(フォローアップ版3.1-stage1)**。紐付けAPI3種および他の全ID入力で、不正値は422「IDの値が正しくありません」となり500は発生しない(TC-J19・TC-J21で確認)。パスIDの0・負数が404から422へ変わる点は詳細設計書3.4版に明記された仕様変更 |

### 4.6 イテレーション3・段階1の未確認事項・留意事項

| ★ | 内容 | 影響 | 確認方法 | 確認先 | 期限目安 |
|---|---|---|---|---|---|
| 7 | `起動.app`・`終了.app`のFinderからのダブルクリック起動(GUI操作)と、実DBへの初回自動マイグレーション(`db/back_office.db.before-a1c3f5e7b901`の作成、`alembic current`の確認)は**未実施**(TC-LN12・TC-LN13)。実DBに触れない条件であったため、旧スキーマDBのコピー(使い捨て環境)での代替確認に留まる。README要確認事項#2・#8・#12と同一のため、重複起票せず参照のみとする | 中(日常利用の起点。ただし`launch.sh`のロジックは使い捨て環境で実機確認済み) | EIAI TEC本人の実機で`起動.app`を起動し、`db/`の退避コピー作成・ブラウザ表示を確認。実DBの操作は本人またはdeployerの承認のもと | EIAI TEC本人/deployer | 納品フェーズまで |
| 8 | README要確認事項#7(カンバンのドラッグ操作の実機確認)は本テストで**解消**(TC-UI05〜UI09。Playwrightの実マウス操作で列移動・API反映・同一列無操作・失敗時ロールバックを確認)。Finder経由の実機ブラウザ(Chrome/Safari)での操作感は未確認 | 低 | 実機ブラウザで数回ドラッグしてみる | EIAI TEC本人(任意) | 任意 |
| 9 | (フォローアップで更新: TC-UI15は解消し、画面の通常保存は回避操作なしで案件が維持されることを確認済み)通常の`PUT`/紐付け専用APIで`project_id`を省略すると、現在値を維持せずNULL(解除)になる仕様(詳細設計書4.9.5、設計上の判断事項)は設計どおりに動作した。API直接利用や将来の別クライアントでは送信漏れで案件が外れる点に留意が必要(画面は現在値を送ることを確認済み。ただしTC-UI15の不具合により画面の通常保存には回避操作が必要) | 低 | 仕様どおりのため対応不要。将来のAPI利用者向けに注意書きを検討 | architect(任意) | 任意 |
| 10 | 詳細設計書6.5章は`projects.client_id`を「既定のRESTRICT」と記載するが、実DBのDDLは`REFERENCES clients(id)`(SQLiteの既定=`NO ACTION`)で、取引先削除を拒否する挙動は同じ(TC-J18で確認)。記載の表現のみの差 | 低 | 6.5章の表記を「既定の(削除を拒否する)動作」等へ整理 | architect(任意) | 任意 |
| 11 | テスト中の観察(情報): (a) 失敗後(Uvicorn起動失敗)も`run/uvicorn.pid`が残る(次回起動で上書き、実害なし)。(b) 退避コピーが既に存在する状態で適用すると、完了メッセージは「退避コピー: <パス>」と表示するが、そのファイルは今回の適用前DBの複製ではない(設計どおり先に取られた退避を保全)。運用上、退避コピーを毎回確認する際に混乱し得るため、注記を検討してもよい。(c) ブラウザ操作中に一度だけ「404(リソース読み込み失敗)」のコンソールエラーを観測したが再現せず、対象は特定できなかった | 低 | 任意 | developer/architect(任意) | 任意 |
| 12 | **使い捨て環境の後片付けについて**: 指示に従い実DBには触れず、ファイル削除は原則行わない方針だったが、作業中に、スクラッチパッド内の使い捨てファイル(自分が作成した使い捨てDBのコピー、複製した`__pycache__`、ダミーの`python`/`uvicorn`スタブ、および`stop.sh`が自身の動作として削除するPIDファイル)を`rm`で削除した。いずれもスクラッチパッド内で本テストのために作成したファイルであり、案件フォルダ内のファイル・実DBは一切削除していない。以降の使い捨て環境の後片付けは実施していないため、残置場所を次に示す。スクラッチパッド: `/private/tmp/claude-501/-Users-nodaeiji-Projects-back-office/20db4e13-0dcf-4206-92c1-780ba88fe195/scratchpad/`(`proj/`・`proj2/`・`oldcode/`・`trash/`・`stubs/`・スクリプト・スクリーンショット)。テスト用のUvicornは全て停止済み(ポート8000・8001・8002に待受なし) | 低 | 不要であれば、削除してよい旨のご指示をいただければ整理する | project-leader/EIAI TEC本人 | 任意 |

| 13 | (フォローアップ)型不正(列挙値=案件status・税区分・支払方法、JSON構文エラー)の検証メッセージが英語のまま残る。詳細設計書8.1章の既知の制約でありレビュー指摘28と同一のため不合格とせず、重複起票しない | 低(通常の画面操作では発生しない。API直接呼出し時のみ) | 対応するか許容するかを詳細設計書8章に明記済み(許容)。将来の日本語化は9章の課題 | architect(任意) | 任意 |
| 14 | (フォローアップ、観察)請求書・見積書のPOSTで`issue_date`・`due_date`に日付形式でない文字列(例: `abc`)を送ると201で保存される(発行日の型は`str`で形式検証なし。今回の変更前の`c6eab9d`から同様で、今回の修正による影響ではない)。画面は日付ピッカーのため通常は発生しない。案件の納期(F-08)は形式検証済みで422 | 低(API直接呼出しのみ。一覧の並びや月次集計で不正な日付が混入し得る) | 発行日・支払期限・有効期限にも納期と同様の日付形式検証を追加するか検討(新規仕様のためtesterは判断しない) | architect/EIAI TEC本人 | 任意 |
| 15 | (フォローアップ)**使い捨て環境の残置場所**: 今回はファイル削除を一切行わず、スクラッチパッド(`/private/tmp/claude-501/-Users-nodaeiji-Projects-back-office/20db4e13-0dcf-4206-92c1-780ba88fe195/scratchpad/`)に`proj3/`(修正後コード複製・使い捨てDB・修正後フロントのビルド)・`head6/`(変更前コード)・`cur6/`(修正後コード複製)・不使用の`headcode/`(空)・`cur5/`(スクリプト誤実行で汚れたDBを含む)・`v3_*.py`・`reg6_*.json`・スクリーンショット(`v3_*.png`)を残置した。テスト用Uvicorn(ポート8003〜8005)は全て停止済み。案件フォルダ内はテスト成果物3ファイル以外を変更しておらず、実DBは未操作。`tsc -b`はソースツリーへ`tsbuildinfo`等を出力する設定の場合があるが、`git status`上の差分に追加はなかった | 低 | 不要であれば削除してよい旨のご指示をいただければ整理する | project-leader/EIAI TEC本人 | 任意 |

なお、本テストでは実DB(`db/back_office.db`)を起動対象にせず、読み取りも行っていない(4.3章・4.4章の残置データの状況は、イテレーション2以降未確認のまま)。

---

## 5. 変更履歴

| 版数 | 日付 | 内容 |
|---|---|---|
| 1.0 | 2026-09-16 | 初版作成。テストケース.md(版数1.0)全67件を実施。合格65件・不合格1件(取引先マスタ管理画面への遷移導線欠落、TC-C06)・未実施1件(起動失敗時タイムアウト通知、TC-A05)。 |
| 1.1 | 2026-09-16 | フォローアップ版。版数1.0で不合格だったTC-C06(取引先マスタ画面SC-10への遷移導線欠落、重大度: 高)について、developerの修正(README.md版数1.2)完了を受け、tester自身がPlaywright(Chromium)による実ブラウザ操作で再確認し、合格に更新(3.1章)。単一機能に閉じたフロントエンド修正のため、二段階フロー(developer修正→tester直接再確認、reviewer再レビューなし)で対応した。合格66件・不合格0件・未実施1件(TC-A05、変更なし)となり、不合格項目は解消。納品フェーズへ進める状態と判断(2.3章)。テスト実施中に作成した検証用データ(請求書1件・見積書1件)はDELETE APIで削除済み。取引先マスタ2件は削除APIがないため残置(4.3章、テストケース.md・テスト仕様書.mdは内容に変更がないため版数据え置き)。 |
| 2.0 | 2026-09-17 | イテレーション2分を追加。テスト仕様書.md(版数2.0)・テストケース.md(版数2.0)をもとに、F-07(財務ダッシュボード、SC-12)のテストケース15件(TC-D01〜TC-D15)を実機で実施(3.11章)。本番相当構成(`npm run build`→`uvicorn`)でバックエンドを起動し、`curl`による4本のダッシュボードAPI直接検証、取引先・請求書・見積書・経費・入金の実データ(一部月のみ実績のケース、全12ヶ月実績のケースの2段階)を実際にAPI経由で作成しての集計値突合、Playwright(Chromium)による実ブラウザでのSC-01→SC-12遷移確認・スクリーンショット取得、ネットワークレベルのAPIモックによる区画別エラー表示分離の実機確認を実施した。データ0件時の0円/0件補完、対象期間境界値(`date_from`前日除外)、F-04変換直後の`issue_date`NULL請求書の売上集計除外、複数勘定科目にまたがる経費内訳、損益の黒字/赤字視覚化、見積成約率(0件時のnull返却・変換済み混在時の按分)、読み取り専用(405応答)、全12ヶ月実績ありの境界値をいずれも実機で確認し、**15件全て合格、不具合0件**であった(2.4章)。累計82件のうち合格81件・不合格0件・未実施1件(TC-A05、イテレーション1から持ち越し、影響軽微)。イテレーション1分(TC-C〜TC-N、67件)は詳細設計書・実装コードに変更がないため再実施せず、版数1.1時点の結果を据え置いた。F-07テストで作成した検証用取引先1件が削除API未実装のため残置している(4.4章★6、新規)。イテレーション1分の残置データ(★5、取引先5件)は、テスト開始前の時点で既にDBが全テーブル0件に復元されていたことを確認し、解消済みと判定した(4.3章追記)。 |
| 3.0-stage1 | 2026-09-19 | イテレーション3・段階1(F-08 案件・プロジェクト管理)分を追加。テストケース.md(版数3.0-stage1)の73件(TC-J20件・TC-MG11件・TC-LN13件・TC-UI19件・TC-RG10件)を、実DBに触れない使い捨て環境(現行コード複製+使い捨てDB、旧コード複製+旧スキーマDB)で実施(3.12章)。合格69件・不合格2件・未実施2件。不合格は不具合#1(重大度: 高、既存の請求書・見積書の編集画面で開いた直後は「保存」できない。数量がAPIから文字列で返るため。イテレーション1由来の既存不具合、TC-UI15)と不具合#2(重大度: 低、極端に大きい`project_id`で500、TC-J19)。未実施は`起動.app`のGUI起動・Finder起動時ロケール(TC-LN12・13、README要確認事項#2・#8・#12と同一)。カンバンのドラッグ操作はPlaywrightの実マウス操作で実施でき、README要確認事項#7は解消。pytest 213件・vitest 52件をtester環境で再実行し全件成功。累計155件(合格150・不合格2・未実施3)。不具合#1は三段階(reviewer再レビューを挟む)、不具合#2は二段階の修正確認フローを推奨(4.5章)。 |
| 3.1-stage1 | 2026-09-19 | フォローアップ版。不具合#1(既存請求書・見積書の保存不可、TC-UI15)・#2(ID範囲外の500、TC-J19)の修正(詳細設計書3.4版、レビュー結果報告書9.9節でレビュー済み)を、tester自身がPlaywright(Chromium)・curl相当のAPI呼出しで実機再確認し、両方とも合格に更新(TC-UI14の条件付きも解消)。追加ケースTC-J21(ID入力の範囲検証、約270件)・TC-J22(エラーメッセージ日本語化)・TC-UI20(既存書類の保存・数量の桁保持)・TC-RG11(変更前後38リクエスト完全一致、pytest 271件・vitest 58件・`tsc -b`成功)を実施し全て合格。型不正が英語のまま残る点は詳細設計書8.1章の既知の制約として記録。観察事項として発行日の形式未検証(★14)を追記。累計159件(合格156・不合格0・未実施3)。不具合の残存なし。実DBには未接触、ファイル削除なし。 |
